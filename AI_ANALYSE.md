# Задача №8: алгоритм обработки сигнала — анализ

Исходные данные (TASK.md, Задача №8): 4-квадрантный датчик 14 мм, отражённый луч
λ = 1.064 мкм, диаметр пятна 2–3 мм (радиус w ≈ 1–1.5 мм по уровню 1/e²),
засветка квадранта S ≈ 0–20 (засвечен) … 3500 (не засвечен). Засветка — это
**энергия**, попавшая на квадрант, а не геометрическое перекрытие площадью.

Цель: определить углы отклонения луча. Вероятный алгоритм: в центре считать углы
текущим алгоритмом `stream` (с реальным размером пятна); при уходе энергии в
1–2 квадранта — точка на краю дисплея как сигнал «покинули центр, менять
направление».

## 8.0. Краткий вывод

Предложенный алгоритм физически корректен и **по большей части уже реализован**
в текущем пайплайне — в том числе «точка на краю» возникает в нём сама собой
(см. §8.2). Главное, чего не хватает: (1) **явного признака зоны** — сейчас
кадр «насыщение» внешне неотличим от кадра «точное измерение»; (2)
**гистерезиса** на границе зон; (3) **памяти направления** при полной потере
сигнала — красная точка в центре («нет сигнала») не говорит, куда возвращать
луч, и визуально похожа на «луч в центре». Датчик принципиально работает как
**нуль-индикатор**: точные углы доступны только в окне ≈ ±1–3°, дальше — лишь
знак отклонения (bang-bang управление), и алгоритм должен честно это
показывать.

## 8.1. Три зоны работы датчика (расчёт)

Нож-модель (как в `deflection_angles`): `D = erf(√2·d/w)`, `d = w·erfinv(D)/√2`,
`θ = atan(d/FOV)`, FOV = 50 мм. Окно измерения определяется насыщением erf —
при `|D| → 1` производная `dD/dd → 0` и обратная задача теряет точность.

| w, мм | \|D\|=0.9 → θ | \|D\|=0.999 (≈`_D_CLIP`) → θ | чувствительность в нуле |
|-------|--------------|------------------------------|--------------------------|
| 1.0   | 0.94°        | 1.88°                        | ΔD=0.01 → Δθ ≈ 26″       |
| 1.25  | 1.18°        | 2.36°                        | ΔD=0.01 → Δθ ≈ 32″       |
| 1.5   | 1.41°        | 2.83°                        | ΔD=0.01 → Δθ ≈ 39″       |

Геометрический край датчика: `atan(7/50) ≈ 8.0°` (полная потеря — около
`atan((7+w)/50) ≈ 9.4°` при w = 1.25). Отсюда три зоны **по каждой оси
независимо**:

1. **ИЗМЕРЕНИЕ** (`|D| ≲ 0.9`, |θ| ≲ 1–1.4°): erf-модель даёт точный угол;
   чувствительность в нуле очень высокая (десятки угловых секунд на 1% сигнала).
2. **НАСЫЩЕНИЕ** (`0.9 ≲ |D| < 1`, |θ| до ≈ 2–3°): угол ещё вычислим, но шум
   усиливается erfinv-ом многократно — оценку показывать, но помечать
   ненадёжной.
3. **ПОТЕРЯ ПОЗИЦИИ** (`|D| = 1`, 3° ≲ |θ| ≲ 8°): пятно целиком в одном
   полудетекторе. Сигнал не меняется при дальнейшем смещении — известен только
   **знак**. Это и есть зона «точка на краю»; единственно возможное управление —
   bang-bang (двигаться к центру до возврата в зону 2).
4. (вырожденный случай) **НЕТ СИГНАЛА** (все s = 4096, |θ| ≳ 9°): луч ушёл с
   датчика — направление возврата из текущего кадра неизвестно в принципе.

Важно: окно точного измерения (±1–3°) — это лишь **малая центральная часть**
датчика (±8°). Бо́льшая часть площади даёт только знак. Это не дефект алгоритма,
а физика квадрантного датчика с пятном много меньше его размера.

## 8.2. Анализ предложенного алгоритма

**Что уже работает.** Центральная ветка («считаем углы алгоритмом stream с
реальным размером пятна») реализована: `make_point` → `deflection_angles` с
`fixed_radius` из калибровки (Задача №5). «Точка на краю дисплея» тоже уже
**возникает сама**: `light_direction_to_point` ставит точку в
`x_norm · size/2`, и при `|x_norm| = 1` (вся энергия в одной половине) точка
оказывается ровно на краю. Дописывать отдельную логику «поставить точку на
край» не нужно — нужно лишь **показать**, что кадр находится в зоне 3.

**Критерий «энергия в 1–2 квадрантах» стоит заменить на насыщение |D|.**
Подсчёт засвеченных квадрантов (`nz` через `quadrant_fracs` и пороги
`S_VAL_MAX/S_VAL_MIN`) опирается на **абсолютные** значения АЦП — он плывёт с
мощностью лазера, отражательной способностью цели и расстоянием. Нормированные
разности `x_norm`, `y_norm` — отношения, они инвариантны к мощности (масштаб
сокращается в делении на S, см. CLAUDE.md «Why a single adc_max works»).
Правильный критерий зоны: `|x_norm| > D_sat` и `|y_norm| > D_sat` **раздельно
по осям**.

**По-осевая обработка обязательна.** «Вся энергия в 2 квадрантах» — это
насыщение только одной оси: если пятно лежит на горизонтальной границе, но
сильно смещено вправо, то `x_norm = 1` (потеря по x), а `y_norm` всё ещё
измеряет точный угол по y. Глобальный признак «покинули центр» выбросил бы
рабочую информацию по второй оси.

**Семантика «энергия, а не площадь» подтверждает выбор модели.** erf-модель —
это в точности доля **энергии** гауссова пучка по одну сторону границы, т.е.
описание задачи совпадает с уже используемой моделью. Покадровый геометрический
решатель `solve_xyr2` (top-hat, площади перекрытия) с этой семантикой как раз
расходится — ещё один довод в пользу постоянного радиуса из калибровки как
основного пути и erf-углов как основного выхода.

**Слепые места предложенного алгоритма:**

- *Дребезг на границе зон.* Около `|D| = D_sat` шум будет гонять индикатор
  туда-сюда; «сигнал для смены направления» начнёт осциллировать.
- *Потеря сигнала неотличима от центра.* Сейчас «нет сигнала» — красная точка
  в **центре** (Frame.no_signal). Для оператора/контура управления это худшая
  возможная индикация: луч полностью ушёл с датчика, а картинка похожа на
  «почти попали». Из самого кадра направление возврата не извлекается — нужна
  память последнего известного знака.
- *Диагональный случай.* Вся энергия в одном квадранте — насыщены обе оси;
  знаки возврата берутся из номера квадранта (максимального s), это покрывается
  по-осевыми знаками `x_norm`, `y_norm` автоматически.

## 8.3. Предлагаемые улучшения

1. **Конечный автомат зон по каждой оси** (x и y независимо):
   `MEASURE → SATURATED → LOST → NO_SIGNAL` с критериями из §8.1
   (`D_lin ≈ 0.9`, `D_sat ≈ 0.98…0.999`; пороги — в `config.py::SensorConfig`).
2. **Гистерезис (триггер Шмитта)** на переходах: вход в насыщение при
   `|D| > 0.98`, выход — только при `|D| < 0.9`. Плюс лёгкая фильтрация D
   (медиана/EMA по 3–5 кадрам) перед классификацией зоны.
3. **Явные признаки в Frame и индикация**: например, `sat_x: bool`,
   `sat_y: bool` (или общий `zone`); в `display_points_stream` — точка на краю
   оранжевая/красная, строка углов `θx/θy` в зоне насыщения сереет (по той же
   схеме, что уже сделано для серого круга `spot_reliable`), в зоне потери —
   вместо числа угла показывать `θx > 2.4°` (нижняя граница, она известна) и
   стрелку направления возврата к центру.
4. **Память последнего знака при потере сигнала**: при переходе в NO_SIGNAL
   хранить последние ненулевые знаки `x_norm`, `y_norm` и показывать их
   (например, красная точка не в центре, а на краю в последнем известном
   направлении, либо стрелка «луч ушёл туда»). Это превращает кадр «нет
   сигнала» из тупика в команду поиска.
5. **Не использовать абсолютные пороги там, где хватает отношений**: зону
   определять по `|D|`; абсолютные `S_VAL_*` оставить только для отличия «есть
   сигнал / нет сигнала» и контроля деградации мощности (сумма яркостей S —
   полезная телеметрия, её стоит выводить в легенду).
6. **Для будущего контура управления** (возврат пятна в центр): в зонах 1–2
   доступно пропорциональное управление по θ, в зоне 3 — релейное (bang-bang)
   по знакам, в зоне 4 — поиск по последнему известному направлению. Выход
   конечного автомата (зона + знаки) — это готовый интерфейс для контроллера
   поворотного столика, его стоит логировать вместе с кадрами (`tee_to_csv`).

## 8.4. Набросок изменений в коде (минимальный)

- `config.py::SensorConfig`: `D_LIN = 0.9`, `D_SAT = 0.98` (+ комментарий со
  ссылкой на §8.1).
- `src/data_types.py::Frame`: поля `sat_x: bool = False`, `sat_y: bool = False`
  (зону по оси кодируют вместе с `no_signal`).
- `src/pipeline/get_single_point.py::make_point`: после вычисления
  `x_norm/y_norm` — классификация зон с гистерезисом (состояние между кадрами —
  рядом с warm-start решателя); углы по насыщенной оси либо `None`, либо
  с флагом.
- `src/visualization/display.py`: цвет точки/строки углов по зоне; стрелка
  возврата; индикация последнего направления при `no_signal`.

Контракт пайплайна не меняется: то же «dict s1..s4 → make_point → Frame →
display», новые поля — чистое расширение.


# Калибровка: анализ `calibr_cmd` и расчёта радиуса пятна по калибровке

Анализируются `cli.py::calibr_cmd` → `src/calibration.py::run_calibration`
(процедура нож-сканирования, Задача №4) и
`src/pipeline/calib_radius.py::spot_radius_from_calib` (расчёт радиуса,
Задача №5). Выводы проверены численно на реальных данных
`DATA/CALIB/CALIBRATE_5.json` и `CALIBRATE_10.json`.

## 9.0. Краткий вывод

Сама нож-модель выбрана и записана **правильно** (`D = erf(√2·x/w)` — точная
доля энергии гауссова пучка по сторонам границы; перевод в пиксели через
`CALIB_PX_PER_MM` корректен). Но в расчёте есть одна **математическая ошибка**
(поправка центра вычитается не в том пространстве) и одна **методическая
несогласованность** (угол центра не вычитается из углов боковых точек). Главная
же проблема — в самой процедуре `run_calibration`: **боковая фиксация
срабатывает преждевременно**, во время вращения столика, и реальные
калибровочные файлы несут следы именно этой систематики (§9.3). Итоговый
w ≈ 4.3 мм при ожидаемых из оптики 1–1.5 мм, скорее всего, артефакт процедуры,
а не свойство пятна.

## 9.1. Что считается корректно

- Модель `gauss_1e2`: `D = erf(√2·x/w)` ⇒ `w = √2·x/erfinv(D)` — верна для
  гауссова пучка с радиусом w по уровню 1/e²; «засветка = энергия» (Задача №8)
  ей соответствует.
- `x = FOV·tan(θ)` — верно как смещение центра пятна в плоскости датчика, если
  `FOV` = расстояние линза–датчик (50 мм): главный луч идёт через центр линзы
  без преломления, поэтому смещение определяется именно этим расстоянием, а не
  положением фокуса.
- Защита `_D_MIN < |D| < _D_MAX` от вырождения erfinv у 0 и ±1 — правильная.
- `normalize_deg` (357.5° → −2.5°) — правильная раскрутка показаний столика.
- Усреднение по всем боковым точкам и пропуск негодных — разумно;
  `spot_radius_from_calib` уже умеет работать с любым числом точек `i1..iN`
  (перебирает все ключи, кроме `i0`) — это пригодится для улучшений §9.4.

## 9.2. Ошибки в `spot_radius_from_calib`

**(а) Поправка центра в пространстве D вместо пространства аргумента.**
Код считает `erfinv(D_i − D_0)`, тогда как модель `D = erf(√2·s/w)` требует
вычитания **аргументов**: `erfinv(D_i) − erfinv(D_0) = √2·(s_i − s_0)/w`.
Вычитание самих значений erf корректно только при `D_0 ≈ 0`; смещение центра
растягивается нелинейностью erf. На реальном `CALIBRATE_10.json`
(`D_0 = −0.0702`) текущий код даёт w(i1) = 3.61 мм и w(i2) = 5.04 мм — разброс
**40%** между симметричными точками создан самой формулой. Ещё лучше —
**двухточечная разность** `w = √2·(x_1 − x_2)/(erfinv(D_1) − erfinv(D_2))`:
смещение нуля сокращается точно, центр вообще не нужен в формуле (он остаётся
полезен только как контроль). На тех же данных она даёт устойчивые 4.23–4.28 мм
для обоих файлов.

**(б) Угол центра не вычитается.** Код корректирует ноль по `D_0`, но угол
берёт абсолютный: `θ_i`, а не `θ_i − θ_{i0}`. Если столик в центре стоит не на
0 (а его угол честно записывается в JSON и потом не используется), смещение
`x = FOV·tan(θ)` считается от чужого нуля. Нужно `Δθ_i = θ_i − θ_{i0}` — тогда
поправки и по D, и по θ согласованы.

**(в) `abs(D)` и `abs(θ)` глотают проверку знаков.** Знаки D и θ обязаны
совпадать (правое положение — положительные оба). Несовпадение — признак
перепутанной раскладки `ph↔s` или знака столика, и его надо репортить, а не
молча «исправлять» модулем.

**(г) Возможный множитель 2 от зеркала.** `x = FOV·tan(θ)` предполагает, что
угол столика равен углу отклонения **луча**. Если столик поворачивает зеркало,
оптический угол равен `2θ`, и w завышается вдвое. Это свойство стенда, его
нельзя вывести из данных — нужен явный коэффициент в `config.py`
(например, `CALIB_ANGLE_SCALE = 1` или `2`) и фиксация геометрии в README.

**(д) Мелочь:** комментарий `fov = float(cfg.FOV)  # м` — на самом деле мм.
И `radius_px` масштабируется через `cfg.SIZE_DISPLAY`, а не через `--size`
команды `log` — при нестандартном `--size` круг будет не в масштабе.

## 9.3. Главная систематика: преждевременная фиксация (по реальным данным)

В обоих калибровочных файлах боковые `x_norm` подозрительно равны порогу
фиксации: **D ≈ 0.458…0.467 при `CALIB_SIDE_EPS = 0.45`**. Условие `_pos_ok`
для боковых точек одностороннее (`x_norm > 0.45`), а `CALIB_HOLD = 8` кадров
набирается за доли секунды — фиксация и усреднение происходят **в момент
пересечения порога, пока столик ещё вращается** к целевым ±1.5°. Дальше
оператор доводит столик до 1.5° и вводит этот угол — в расчёт попадает пара
«D с угла ~0.4°, θ = 1.5°».

Проверка согласованности: при заявленном из оптики пятне 2–3 мм (w ≈ 1.25 мм)
на θ = 1.5° ожидается `D = erf(√2·50·tan1.5°/1.25) ≈ 0.96`, а измерено 0.46.
Зато D = 0.46 при w = 1.25 мм соответствует θ ≈ 0.44° — то есть данные отлично
объясняются захватом в момент пересечения порога. Полученное w ≈ 4.3 мм —
арифметически честный результат по противоречивой паре (D, θ).

Дополнительные симптомы в тех же файлах, которые расчёт никак не проверяет:

- **P падает на 22%** между центром (14242) и боковыми точками (~11130) — при
  пятне целиком на датчике суммарная энергия должна сохраняться. Засвеченные
  квадранты в боковых точках читают s ≈ 30 при нижней границе S_VAL_MAX ≈ 0–20 —
  возможно ограничение (клиппинг) АЦП/усилителя.
- «Тёмные» квадранты в боковых точках читают s ≈ 2400–2800 (яркость 1300–1700
  при «темноте» ≈ 3500) — заметный фон/ореол. Фон, равный на обеих половинах,
  сжимает D (`D_изм = D_ист·P_пятна/(P_пятна+P_фона)`) и завышает w.

## 9.4. Улучшения `calibr_cmd` / `run_calibration`

1. **Исправить критерий боковой фиксации (критично).** Фиксировать только
   стабильное положение: например, `CALIB_HOLD` кадров с разбросом
   `max−min` по `x_norm` меньше ~0.01 (столик остановлен), а не просто
   `x_norm > 0.45`. Альтернатива ещё надёжнее — подтверждение клавишей по
   образцу режима `measure` («наведите, остановитесь, нажмите w»): оператор
   сам решает, когда положение установлено.
2. **Считать и показывать w сразу после каждой точки.** Сейчас расчёт офлайн, и
   о браке калибровки узнают позже. После ввода угла вызвать тот же расчёт,
   печатать w по точке и разброс по парам; предупреждать, если D близко к
   порогу фиксации, если |w(i1)−w(i2)| велик, если знаки D/θ не согласованы,
   если P между точками плывёт больше ~10%.
3. **Больше точек.** Снимать 5–9 углов (например, 0, ±0.5°, ±1°, ±1.5°) и
   фитовать `D(θ) = erf(√2·FOV·tan(θ−θ₀)/w)` по (w, θ₀) МНК —
   `spot_radius_from_calib` уже перебирает произвольные `i1..iN`, формат JSON
   менять не надо. Это даёт и оценку погрешности w, и проверку модели по
   остаткам.
4. **Тёмновой кадр.** Отдельный шаг «перекройте луч»: записать фоновые s1..s4 и
   вычитать фон из яркостей при расчёте D — устраняет систематику §9.3
   (сжатие D фоном).
5. **Вертикальный скан.** Та же процедура по `y_norm` (верх/низ) — даёт w по
   второй оси и выявляет астигматизм; сейчас радиус молча считается изотропным.
6. **Файл результата.** `save_calibration` обещает в докстринге
   `CALIB_{timestamp}.json`, а пишет всегда `CALIBRATE.json`, затирая прошлую
   калибровку. Писать таймстампную копию + обновлять `CALIBRATE.json` как
   «текущую». Полезно также сохранять `adc_max`, разброс s по кадрам и версию
   процедуры.
7. **Повтор шага.** Возможность пере-снять точку (`r`) без перезапуска всей
   процедуры и подтверждение введённого угла (опечатка в углу сейчас молча
   портит калибровку).

## 9.5. Улучшения `spot_radius_from_calib`

1. Перейти на **разности в пространстве erfinv**: попарно
   `w = √2·(x_i − x_j)/(erfinv(D_i) − erfinv(D_j))` по всем парам точек (или
   МНК-фит из §9.4 п.3); `i0` использовать как контроль нуля, а не как
   вычитаемое в D-пространстве.
2. Использовать `Δθ = θ_i − θ_{i0}` (угол центра уже записан в JSON).
3. Ввести `CALIB_ANGLE_SCALE` (×2 для зеркала) в `config.py`.
4. Валидация вместо `abs()`: проверять согласованность знаков D и θ,
   предупреждать о точках с D у порога фиксации и о разбросе w между точками
   (возвращать его в `per_point` — частично уже есть).
5. Поправить комментарий «# м» → «# мм».

## 9.6. Итог (см. уточнения в §9.7)

Формула радиуса в основе корректна, но текущая реализация даёт смещённый
результат по двум независимым причинам: математической (поправка центра в
D-пространстве — до 40% разброса на реальных данных) и процедурной
(преждевременная фиксация — вероятно, главный вклад: w ≈ 4.3 мм вместо
ожидаемых 1–1.5 мм). Минимальный набор исправлений: стабильность/подтверждение
при фиксации (§9.4 п.1), двухточечная формула в erfinv-пространстве (§9.5 п.1),
Δθ от центра (§9.5 п.2) и мгновенный контроль w после съёмки (§9.4 п.2). После
этого стоит пере-снять калибровку и сверить w с оптической оценкой из
Задачи №6.

## 9.7. Уточнения и статус реализации (2026-06-10)

Уточнения по результатам обсуждения:

1. **Пятно в `CALIBRATE_*.json` было неизвестного размера**, поэтому w ≈ 4.3 мм
   может быть корректным результатом, а не артефактом — гипотеза §9.3 о
   преждевременной фиксации остаётся именно риском процедуры (D ≈ порогу — лишь
   косвенный признак), и этот риск устранён переходом на подтверждение «w».
2. **Столик ходит только по оси x** — расчёт остаётся по двум крайним точкам
   (i1, i2); многоточечный скан (§9.4 п.3) опционален, вертикальный скан
   (§9.4 п.5) неприменим.

Реализовано:

- §9.4 п.1 — фиксация в `run_calibration` только по клавише «w» (автозахват по
  порогу убран; зелёная точка осталась как подсказка зоны); после подтверждения
  усредняются `CALIB_HOLD` кадров с контролем дрейфа D и слабого сигнала.
- §9.4 п.2 — после каждой снятой точки печатается текущая оценка w
  (`_report_radius` → `spot_radius_from_points`, та же математика, что офлайн).
- §9.5 полностью: разности в erfinv-пространстве (основная оценка — пара
  i1−i2, в ней смещение нуля сокращается точно; центр — опора нуля угла и
  диагностика), `Δθ = θ_i − θ_{i0}`, `cfg.CALIB_ANGLE_SCALE` (×2 для зеркала,
  по умолчанию 1), валидация знаков D/Δθ и предупреждения (D у порога фиксации,
  разброс оценок >20%, смещённый центр) вместо молчаливого `abs()`; комментарий
  «м» исправлен. `spot_radius_from_calib` возвращает дополнительно
  `warnings` — `cli` печатает их при старте `log`/`stream`.


# Диаметр пятна по квадрантам и его онлайн-коррекция — анализ

Вопросы: (1) можно ли рассчитать диаметр пятна по значениям квадрантов и какие
ещё данные нужны; (2) как корректировать размер пятна в процессе работы, если
он меняется (размер нужен для расчёта углов поворота).

## 10.1. Что содержится в одном кадре: размер принципиально вырожден

Для гауссова пятна с центром (x₀, y₀) и радиусом w доли энергии в квадрантах
факторизуются:

    F(±x, ±y) = ¼ · (1 ± Dx) · (1 ± Dy),   Dx = erf(√2·x₀/w),  Dy = erf(√2·y₀/w)

Проверено численно: пятна (x₀=0.5, w=1) и (x₀=2, w=4) дают **в точности
одинаковые** четыре доли. Из четырёх значений s1..s4 извлекаются только две
независимые величины — Dx и Dy (третья комбинация, «диагональная»
(F₁+F₃−F₂−F₄), тождественно равна Dx·Dy и новой информации не несёт; четвёртая —
полная сумма S, которая от w не зависит, пока пятно целиком на датчике).

Вывод: **один кадр измеряет только отношения x₀/w и y₀/w** — смещение в
единицах радиуса пятна. Сам w в данные не входит; никакая обработка одного
кадра его не извлечёт. Это не недостаток алгоритма, а свойство гауссова
профиля: он масштабно-инвариантен, в кадре нет «линейки».

Почему покадровый решатель `solve_xyr2` всё-таки выдаёт радиус: он предполагает
**top-hat** (равномерный круг с резкой границей). У top-hat линейка есть — его
собственный край и край/размер детектора, доли площадей зависят от R отдельно
от x₀. Но если реальный профиль ближе к гауссову (а семантика «засветка =
энергия» из Задачи №8 на это указывает), top-hat-радиус — лишь эффективная
величина, систематически смещённая.

Полезный побочный продукт: тождество «диагональ = Dx·Dy» можно проверять в
каждом кадре — заметное нарушение означает, что модель (гаусс, всё пятно на
датчике, нет клиппинга АЦП) не выполняется, и доверять erf-углам в этом кадре
нельзя. Это бесплатный покадровый индикатор достоверности.

## 10.2. Какие дополнительные данные дают размер («линейка» извне)

1. **Известное смещение** Δx = FOV·tan(Δθ) от поворотного столика — нож-метод,
   уже реализован (калибровка, §9). Минимум — две точки с известной разницей
   углов: `w = √2·(x₁−x₂)/(erfinv(D₁)−erfinv(D₂))`.
2. **Известная модуляция (dither).** Малое колебание луча с известной
   амплитудой δx на частоте выше полосы слежения; демодуляция D даёт локальный
   наклон dD/dx, а вместе со значением D — радиус:
   `w = (2√2/√π)·e^(−erfinv(D)²) / (dD/dx)`.
   Работает непрерывно в любой точке линейной зоны; требует привода с
   модуляцией.
3. **Известная форма с резким краем** — допущение top-hat (текущий
   `solve_xyr2`); корректно лишь насколько профиль действительно резкий.
4. **Геометрия датчика как линейка**: спад суммы S при выходе хвостов пятна за
   край датчика и потери в мёртвой щели между квадрантами зависят от w. Эффекты
   слабые (для w≈4.3 мм на смещениях калибровки — доли процента) — годятся как
   контроль «пятно сильно выросло», не как измерение.
5. **Внешние средства**: отводная пластина + камера (прямое измерение профиля)
   или модель оптики (Задача №6) + дальномер до цели (w как функция дистанции —
   для отражённого луча размер зависит от положения цели).

## 10.3. Онлайн-коррекция w в процессе работы

Ключевое наблюдение: **контур управления сам постоянно делает известные
смещения**. Каждая команда столику/зеркалу Δθ_k — это готовый «нож-скан»:

    w_k = √2 · FOV·tan(Δθ_k) / (erfinv(D_после) − erfinv(D_до))

— та же формула и тот же код, что в `spot_radius_from_points` (пара точек с
известной разницей углов). Калибровка из разовой превращается в непрерывную,
без остановки работы и без нового железа.

Чтобы поток оценок w_k был устойчивым:

- **Ворота качества** (оценка отбрасывается, если): любая из |D| вне
  [~0.1, 0.9] (зоны §8.1); |erfinv(D_после)−erfinv(D_до)| меньше шумового
  порога (слишком малый шаг — при w≈4.3 мм шаг столика 0.1° даёт ΔD≈0.03,
  это разумный минимум); сумма S между кадрами изменилась заметно (смена
  мощности/отражения); рядом была потеря сигнала или зона lost.
- **Фильтрация**: медиана/EMA по последним N годным оценкам (w меняется
  медленно — постоянная времени фильтра может быть секунды); хранить и разброс —
  при большом разбросе не обновлять w и помечать кадры как менее надёжные
  (механизм `spot_reliable` уже есть).
- **Системная ошибка — собственное движение цели**: между «до» и «после» луч
  мог сместиться сам. Лечится отбором быстрых/крупных шагов (командное смещение
  ≫ дрейф за время шага) — несмещённый шум давится фильтром.
- **Режим «зонд»**: когда луч устойчиво в центре и команд нет, периодически
  выполнять намеренную пару микро-шагов ±δθ (суммарно ноль) и снимать оценку —
  контролируемый вариант того же метода для спокойных участков.

Требуемая частота коррекции следует из чувствительности углов: в линейной зоне
`θ ∝ w` (`d = w·erfinv(D)/√2`), то есть **относительная ошибка w переходит в
такую же относительную ошибку угла**. Если допустима ошибка угла 5% — w надо
обновлять раньше, чем он успевает уйти на 5%.

## 10.4. Рекомендация

1. Базово (без нового железа): непрерывная переоценка w по парам «до/после»
   командных шагов контура + зонд-пары в простое; математика уже реализована в
   `spot_radius_from_points`, нужен только сбор пар (D, θ) из контура и
   EMA-фильтр; передавать обновлённый w в `make_point` вместо константы
   `fixed_radius` (например, через изменяемый объект/коллбек).
2. Покадровый контроль модели: проверка тождества «диагональ = Dx·Dy» как
   индикатор достоверности erf-углов.
3. При появлении привода с модуляцией — dither-метод (п.2 §10.2) как полностью
   пассивное слежение за w.
4. Камера на отводе — разовая верификация всех методов и формы профиля
   (top-hat vs гаусс), это снимет и вопрос систематики `solve_xyr2`.
