# Радиус засвечиваемого пятна по квадрантам и его визуализация

> Ответ на задачу: «посчитать по засветке каждого квадранта радиус пятна и
> визуализировать пятно на дисплее». Опорный расчёт — `DOCUMENTATION/spot_plot.py`.

---

## 0. TL;DR (короткий ответ)

1. **Радиус пятна `w` — это калибровочная константа оптической системы, а не
   мгновенная величина.** Из 4 чисел одного кадра (`s1..s4`) восстанавливается
   только *положение* центра пятна (то, что проект уже считает как `x_norm`,
   `y_norm`) и суммарная яркость `S`. Сам *размер* пятна по одному кадру
   квадрантного детектора в общем случае не определяется.
2. Размер получают **по скану** — прогоняют пучок по диапазону известных углов φ
   и фитят характеристику `r = erf(φ / φc)`. Параметр `φc` и есть мера размера
   пятна: радиус по уровню 1/e² равен `w = √2 · φc`. Именно это делает
   `spot_plot.py`.
3. На дисплее пятно рисуется как **окружность фиксированного радиуса** (из
   калибровки `φc`), с центром в уже вычисляемой точке `Point2D`. Опционально —
   яркость/прозрачность окружности модулируется суммой `S`.
4. На вашем логе `DATA/putty.csv` калибровка **уже считается** (там есть свип по
   `v_x ∈ [−2, +18]`): получается `φc_x ≈ 5.67`, `φc_y ≈ 5.48` (R² = 0.96 / 0.99),
   то есть `φc ≈ 5.6`, радиус пятна `w ≈ 7.9` в тех же угловых единицах.

Дальше — подробно: что считает `spot_plot.py`, физика, три способа получить
радиус и как встроить визуализацию в текущий пайплайн.

---

## 1. Что именно считает `DOCUMENTATION/spot_plot.py`

Скрипт — это **оффлайн-калибратор**, а не визуализатор потока. Он:

1. читает строки `phi_x, rx, phi_y, ry, sum` (свип пучка по углам),
2. фитит модель `r = erf(φ / φc)` отдельно по X и Y (`fit_phi_c`),
3. по `φc` выдаёт рекомендации по настройке оптики (`recommendation`).

Ключевые соотношения из скрипта:

| Величина | Формула в скрипте | Смысл |
|---|---|---|
| Характеристика | `r = erf(φ / φc)` | пеленгационная кривая квадрантного детектора |
| Наклон у нуля | `k = 2 / (√π · φc)` (стр. 173) | крутизна `dr/dφ` при φ→0 |
| Линейная зона | `±0.4 · φc` (стр. 91, 178) | где `erf` ≈ линейна (ошибка < 5 %) |
| «Физ. предел» | `±4 · φc` (стр. 92) | эвристика рабочего диапазона |
| Угловой размер | `w/f = φc/1000` (стр. 226) | связь `φc` (мрад) ↔ размер пучка |

Маска достоверности (стр. 138–139): `0.02 ≤ |r| ≤ 0.92` и `sum > 0.05` — отсекает
шум у нуля и насыщение у ±1. Для устойчивого `φc` нужно ≥ 4 точек.

**Важно:** `φc` извлекается из *набора* точек с *известными* истинными углами φ.
По одному кадру `φc` не получить — см. п. 3.

---

## 2. Соответствие переменных `spot_plot.py` ↔ переменные проекта

Проект уже считает ровно те же `rx`, `ry` — просто называет их `x_norm`,
`y_norm` (`src/pipeline/get_single_point.py::light_direction_to_point`):

```
spot_plot.py        проект (get_single_point.py)
-----------         ---------------------------
rx            <-->  x_norm = (p2 + p4 − p1 − p3) / S   = (право − лево) / S
ry            <-->  y_norm = (p1 + p2 − p3 − p4) / S   = (верх  − низ ) / S
sum (норм.)   <-->  S      = b1 + b2 + b3 + b4          (b_i = adc_max − raw_i)
phi_x, phi_y  <-->  v_x, v_y    (см. п. 4 — это, похоже, и есть истинный угол)
```

С учётом раскладки `ph1=s1, ph2=s4, ph3=s2, ph4=s3` (матрица `[[b1,b4],[b2,b3]]`):

```
rx = (b4 + b3 − b1 − b2) / S      ry = (b1 + b4 − b2 − b3) / S
```

То есть «направление», которое проект уже рисует точкой, — это и есть пара
`(rx, ry)`. Не хватает только **размера** пятна.

---

## 3. Физика: квадрант измеряет ПОЛОЖЕНИЕ, а не РАЗМЕР

Модель: гауссово пятно радиуса `w` (по уровню 1/e²), центр `(x0, y0)`, мощность
`P`. Интенсивность `I ∝ exp(−2((x−x0)² + (y−y0)²) / w²)`.

Нормированная разность по ножевой грани в центре даёт ровно `erf`:

```
rx = erf( √2 · x0 / w ) = erf( x0 / φc )      ⇒   φc = w / √2   ⇒   w = √2 · φc
```

Отсюда — главный вывод. По **одному** кадру вы наблюдаете 3 независимые величины:
`S` (мощность), `rx` (≈ x0), `ry` (≈ y0). А у гауссовой модели **4** неизвестных:
`P, x0, y0, w`. Система недоопределена по `w`: разностный сигнал зависит от
*положения* и *размера* в одной и той же связке `x0/w`, разделить их одним кадром
нельзя. Нужен один из источников дополнительной информации:

- **скан** по известным углам φ (тогда `w` = из наклона/насыщения кривой `erf`), или
- **независимый истинный угол** φ для каждого кадра (тогда `w` из `rx = erf(√2·φ/w)`).

Поэтому «радиус» — это либо калибровочная константа (способ 4.1), либо
пер-фрейм оценка при наличии эталонного угла (способ 4.2), либо качественный
прокси (способ 4.3).

---

## 4. Три способа получить радиус

### 4.1. Калибровка `φc` по скану — РЕКОМЕНДУЕТСЯ (истинный физический размер)

Делает ровно то же, что `spot_plot.py`, но на данных проекта. Нужен лог со
**свипом** пучка по диапазону углов (а не стационарная запись). На вашем
`putty.csv` свип есть — проверено, фит сходится:

```
φc_x ≈ 5.67 ± 0.11   (R² = 0.956)
φc_y ≈ 5.48 ± 0.03   (R² = 0.991)
⇒ φc ≈ 5.6  →  радиус пятна w = √2·φc ≈ 7.9   (в единицах v_x/v_y)
   линейная зона ≈ ±2.2 ;  наклон k = 2/(√π·φc) ≈ 0.20 /ед.
```

Готовая функция калибровки (можно положить в `src/pipeline/calibration.py`):

```python
import numpy as np
from scipy.optimize import curve_fit
from scipy.special import erf
import pandas as pd


def calibrate_phi_c(df: pd.DataFrame) -> dict:
    """
    Оценивает φc (меру размера пятна) по скану: фитит rx = erf(v_x/φc),
    ry = erf(v_y/φc). Требует, чтобы v_x, v_y были ИСТИННЫМ углом (эталоном),
    а не повтором rx/ry. Возвращает φc по X, Y и среднее + радиус w = √2·φc.
    """
    adc = float(df[["s1", "s2", "s3", "s4"]].to_numpy().max())
    b1, b2 = adc - df.s1, adc - df.s2
    b3, b4 = adc - df.s3, adc - df.s4
    S = b1 + b2 + b3 + b4
    rx = ((b4 + b3 - b1 - b2) / S).to_numpy()
    ry = ((b1 + b4 - b2 - b3) / S).to_numpy()
    sn = (S / S.max()).to_numpy()

    def fit(phi, r):
        m = (np.abs(r) >= 0.02) & (np.abs(r) <= 0.92) & (sn > 0.05)
        if m.sum() < 4:
            return None
        popt, _ = curve_fit(lambda p, pc: erf(p / pc), phi[m], r[m],
                            p0=[5.0], bounds=(0.1, 100.0), maxfev=5000)
        return float(popt[0])

    pcx = fit(df.v_x.to_numpy(), rx)
    pcy = fit(df.v_y.to_numpy(), ry)
    pc = np.mean([v for v in (pcx, pcy) if v is not None]) if (pcx or pcy) else None
    return {"phi_c_x": pcx, "phi_c_y": pcy,
            "phi_c": pc, "w": (np.sqrt(2) * pc if pc else None)}
```

Результат `φc` сохраняется один раз как константа и используется для отрисовки
окружности фиксированного радиуса (п. 5).

### 4.2. Пер-фрейм оценка через истинный угол (если `v_x/v_y` — эталон)

Если в каждой строке есть *истинный* угол `(v_x, v_y)` (внешний эталон), радиус
можно оценить покадрово, обратив `erf`:

```python
import math
from scipy.special import erfinv

def spot_radius_per_frame(rx, ry, phi_x, phi_y, eps=1e-3):
    # rx = erf(√2·φ/w)  ⇒  w = √2·φ / erfinv(rx)
    out = []
    for r, phi in ((rx, phi_x), (ry, phi_y)):
        r = max(-0.92, min(0.92, r))           # уйти от насыщения
        if abs(r) < 0.02 or abs(phi) < eps:    # около нуля 0/0 — пропустить
            continue
        out.append(math.sqrt(2) * abs(phi) / erfinv(abs(r)))
    return sum(out) / len(out) if out else None
```

Минусы: шумит у `r→0` (0/0) и у `r→1` (`erfinv→∞`), поэтому обязательно
сглаживание (скользящее среднее по N кадров). По сути это «онлайн-версия»
калибровки 4.1; для стабильной картинки лучше калибровка.

### 4.3. Качественный прокси по самим квадрантам (только для наглядности)

Без эталонного угла истинный радиус не получить, но для «живости» картинки можно
менять видимый размер/яркость пятна по наблюдаемым величинам. Это **индикатор, а
не метр**:

- по суммарной яркости: `r_px ∝ S / S_ref` (ярче ⇒ ближе/сфокусированнее);
- по «остроте» отклика: чем дальше `|r| = √(rx²+ry²)` от 1, тем пятно «в зоне».

Использовать только если 4.1/4.2 недоступны, и подписывать как качественную
оценку.

---

## 5. Как нарисовать пятно на дисплее

Идея: оставить текущую **точку** (центр = положение из `rx, ry`) и добавить
**окружность** радиуса `r_px`, представляющую физический размер пятна.

### 5.1. Расширить кадр `Frame` (`src/data_types.py`)

```python
@dataclass
class Frame:
    point: Point2D | None
    v_x: float | None = None
    v_y: float | None = None
    radius: float | None = None   # радиус пятна в пикселях дисплея
```

### 5.2. Прокинуть радиус в `make_point` (`src/pipeline/get_single_point.py`)

Дисплей работает в нормированных единицах: половина окна `size/2` соответствует
`r = 1`. Угловой размер `w = √2·φc` переводим в пиксели одним масштабом
(калибровка п. 4.1) — добавьте `cfg.SPOT_PHI_C` и `cfg.SPOT_SCALE` в `config.py`:

```python
# радиус окружности в пикселях: масштаб подбирается визуально один раз
phi_c = adc_max_unused = None       # см. ниже — phi_c берём из cfg
...
for row in rows:
    ...
    point = light_direction_to_point(matrix, size)
    # вариант A (рекомендуется): постоянный радиус из калибровки
    radius = cfg.SPOT_SCALE * cfg.SPOT_PHI_C        # px
    # вариант C (опц.): лёгкая модуляция по яркости S = sum(b)
    # radius *= min(1.5, (b1+b2+b3+b4) / cfg.SPOT_S_REF)
    yield Frame(point=point, v_x=row.get("v_x"),
                v_y=row.get("v_y"), radius=radius)
```

`cfg` (в `config.py`, класс `DisplayConfig`):

```python
class DisplayConfig:
    SIZE_DISPLAY = 200
    INTERVAL = 0.05
    SPOT_PHI_C = 5.6     # из calibrate_phi_c (п. 4.1)
    SPOT_SCALE = 6.0     # px на единицу φc — подобрать «на глаз» под размер окна
```

### 5.3. Отрисовать окружность (`src/visualization/display.py`)

```python
from matplotlib.patches import Circle

# при инициализации фигуры, рядом с (line,) = ax.plot(...)
spot = Circle((0, 0), 0.0, fill=True, alpha=0.25,
              color="yellow", visible=False)
ax.add_patch(spot)

# в цикле по кадрам, в ветке point is not None:
if frame.radius:
    spot.center = (point.x, point.y)
    spot.set_radius(frame.radius)
    spot.set_visible(True)
else:
    spot.set_visible(False)
# а в ветке point is None: spot.set_visible(False)
```

Получится: жёлтое полупрозрачное пятно (физический размер из калибровки) с белой
точкой-центром (положение), которое ездит по дисплею вместе с направлением
засветки. При желании — цвет/alpha окружности по `S` (интенсивности), как в
правой панели `spot_plot.py` (`cmap='RdYlGn'`).

---

## 6. Подводные камни

- **Раскладка `ph ↔ s` (load-bearing).** Любой новый расчёт `rx, ry` обязан
  использовать матрицу `[[s1, s4], [s2, s3]]` (т.е. `ph1=s1, ph2=s4, ph3=s2,
  ph4=s3`), иначе картинка зеркалится. См. `CLAUDE.md` → «Sensor layout».
- **Инверсия сырого АЦП.** Яркость = `adc_max − raw` (0 = max света). Радиус
  считается по яркостям `b_i`, а не по сырым `s_i`. Не зовите `normalize_data`
  повторно — инверсия уже внутри `make_point` (иначе двойная инверсия).
- **Единицы `v_x/v_y`.** В `putty.csv` это, похоже, истинный угол (свип
  `−2…+18`), а не повтор `rx`. Это гипотеза — **проверьте у источника прошивки**,
  прежде чем доверять калибровке 4.2. Если `v_x` окажется собственным `rx`
  устройства, фит `φc` будет бессмысленным (тождество `erf(rx)≈rx`).
- **Свип vs стационар.** Для калибровки нужен именно прогон пучка по диапазону.
  Стационарная запись (облако точек у одной позиции) `φc` не даёт.
- **Насыщение и ноль.** `erfinv` взрывается у `|r|→1` и даёт 0/0 у `|r|→0` —
  всегда маска `0.02 ≤ |r| ≤ 0.92` и сглаживание (для способа 4.2).
- **Поток (UART) не знает `φc`.** Живой поток размер пятна сам не определит:
  используйте заранее откалиброванную константу `cfg.SPOT_PHI_C` (как `cfg.ADC_MAX`
  для нормировки).

---

## 7. Минимальный план внедрения

1. `src/pipeline/calibration.py` ← функция `calibrate_phi_c` (п. 4.1); прогнать
   на скан-логе, получить `φc` (на `putty.csv` уже ≈ 5.6).
2. Записать `SPOT_PHI_C`, `SPOT_SCALE` в `config.py::DisplayConfig`.
3. Добавить поле `radius` в `Frame` (`src/data_types.py`).
4. Заполнять `radius` в `make_point` (постоянный из `φc`, опц. модуляция по `S`).
5. Рисовать `Circle` в `display_points_stream` (центр = точка, радиус = `radius`).
6. Подобрать `SPOT_SCALE` визуально под размер окна.

Рекомендуемый порядок достоверности: **4.1 (калибровка)** → 4.2 (пер-фрейм при
эталоне) → 4.3 (качественный прокси). Для боевой визуализации — 4.1 + постоянный
радиус, при необходимости подсветка интенсивностью.
