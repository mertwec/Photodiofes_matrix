# Радиус засвечиваемого пятна по квадрантам и его визуализация

> Ответ на задачу: «посчитать по засветке каждого квадранта радиус пятна и
> визуализировать пятно на дисплее». Опорный расчёт — `DOCUMENTATION/spot_plot.py`.

---

## ⚠ Поправка №2 (геометрическая модель площадей пересечения — РЕКОМЕНДУЕТСЯ)

Источник: `DOCUMENTATION/r2_estimation_algorithm.pdf` — «Алгоритм оценки радиуса
перемещаемой окружности по площадям пересечения с квадрантами».

### Почему текущий расчёт (Поправка №1) даёт неверный размер пятна

Поправка №1 считает радиус **только из суммарной засветки**:
`f = mean(b_i)`, `r = size·√(f/π)`. Это неявно предполагает, что **всё пятно
целиком лежит на детекторе** (режим полного вложения, смещение центра `d = 0`).

Но как только свет приходит под углом, центр пятна уходит от центра детектора
(`d > 0`) и **часть пятна вылезает за край** активной зоны. Тогда измеренная
суммарная площадь `A_tot = Σ A_i` оказывается **меньше** истинной площади пятна
`π·R2²`:

```
d = 0   → A_tot = π·R2²   → √(A_tot/π) = R2   (точно)
d > 0   → A_tot < π·R2²   → √(A_tot/π) < R2   (ЗАНИЖАЕТ)
```

Чем сильнее пятно смещено, тем сильнее `√(f/π)` занижает радиус. Поэтому круг на
дисплее «сжимается» при наведении датчика в сторону, хотя физический размер пятна
не меняется — **это и есть наблюдаемая некорректность**. В PDF та же формула
`R2_0 = 1.2·√(A_tot/π)` используется **только как начальное приближение** для
оптимизатора (множитель 1.2 — грубая поправка на занижение при `d > 0`), а не как
итоговый ответ.

### Правильная модель (квадрантный детектор, обратная задача 3D)

PDF моделирует геометрию так (стр. 1–2):

- детектор — **круг** `C1` радиуса `R1 = 1`, разбитый осями на 4 квадранта `Q1..Q4`;
- пятно — **круг** `C2` с центром `(x, y)` и **неизвестным радиусом `R2`**;
- измеряются 4 площади `A1..A4` — **площади пересечения** пятна с каждым квадрантом.

Ключевой момент: по 4 площадям ищутся **сразу 3 неизвестных `(x, y, R2)`** —
переопределённая система (4 уравнения, 3 неизвестных), решается в смысле МНК.
Радиус **нельзя** вытащить из суммы площадей в отрыве от положения — он c ним
связан. Нужно **совместно подгонять `(x, y, R2)`** под все 4 измеренные площади.

**Почему `R2` вообще определим** (стр. 4, якобиан `∂A/∂(x,y,R2)`): изменение `R2`
меняет **и** суммарную площадь, **и** её распределение по квадрантам, поэтому
`∂A/∂R2` линейно независим от `∂A/∂x` и `∂A/∂y` (ранг якобиана = 3). Именно это
позволяет 4 площадям зафиксировать 3 параметра. (`x`/`y` перераспределяют площадь
лево↔право и низ↔верх, не меняя сумму; `R2` меняет и сумму, и форму.)

### Что необходимо для вычисления радиуса засвечиваемой окружности

**1. Прямая модель `A_i(x, y, R2)`** — по центру и радиусу пятна посчитать
площадь пересечения с каждым квадрантом. Численное интегрирование методом средних
прямоугольников по горизонтальным полосам (`N = 500`), **правило средней точки**
(не трапеций — оно убирает ложную асимметрию, когда узел сетки попадает на границу
`y = 0`). Псевдокод из PDF (стр. 2), оформленный под проект:

```python
import math
import numpy as np

def forward_areas(x, y, R2, R1=1.0, N=500):
    """Площади пересечения круга (x,y,R2) с 4 квадрантами круга R1.
    Возврат: A[0..3] = [Q1 верх-право, Q2 верх-лево, Q3 низ-лево, Q4 низ-право]."""
    A = np.zeros(4)
    y_lo = max(y - R2, -R1)
    y_hi = min(y + R2,  R1)
    if y_hi <= y_lo:
        return A                       # нет пересечения по вертикали
    h = (y_hi - y_lo) / N
    for k in range(N):
        yy = y_lo + (k + 0.5) * h                      # середина полосы
        aa = math.sqrt(max(0.0, R1*R1 - yy*yy))        # полуширина детектора C1
        d2 = R2*R2 - (yy - y)*(yy - y)
        if d2 <= 0:
            continue
        b  = math.sqrt(d2)                             # полуширина пятна C2
        xL = max(x - b, -aa)
        xR = min(x + b,  aa)
        if xR <= xL:
            continue
        if xR > 0:                                     # правая половина
            A[0 if yy >= 0 else 3] += (xR - max(xL, 0.0)) * h
        if xL < 0:                                     # левая половина
            A[1 if yy >= 0 else 2] += (min(xR, 0.0) - xL) * h
    return A
```

**2. Измеренные площади `A1..A4`** — это уже считаемые доли засветки квадрантов
`b_i ∈ [0, 1]` (как в `spot_radius_px`), приведённые к единицам детектора `R1 = 1`:
полностью засвеченный квадрант = четверть круга = `π/4`, поэтому `A_i = (π/4)·b_i`.

Раскладка `ph ↔ s` (**load-bearing**, см. `CLAUDE.md`) задаёт, какой датчик идёт
в какой квадрант PDF (`Q1` — верх-право, `Q2` — верх-лево, `Q3` — низ-лево,
`Q4` — низ-право):

```
A1 (Q1 верх-право) = (π/4)·b(s4)      ph2 = s4
A2 (Q2 верх-лево)  = (π/4)·b(s1)      ph1 = s1
A3 (Q3 низ-лево)   = (π/4)·b(s2)      ph3 = s2
A4 (Q4 низ-право)  = (π/4)·b(s3)      ph4 = s3
b(s) = clip((S_VAL_MIN − s) / (S_VAL_MIN − S_VAL_MAX), 0, 1)
```

**3. Обратный решатель** — минимизировать невязку по всем 4 площадям
`F(x, y, R2) = Σ_i [A_i^модель(x,y,R2) − A_i^изм]²` методом **Нелдера–Мида в R³**
(прямой метод без производных, симплекс-тетраэдр; стр. 6). С тремя ингредиентами:

- **начальное приближение** (стр. 7): `x0 = 1.5·((A1+A4) − (A2+A3))` (право−лево),
  `y0 = 1.5·((A1+A2) − (A3+A4))` (верх−низ), `R2_0 = 1.2·√(A_tot/π)`;
- **многостарт** (стр. 8, 38 стартов: сетка 9 позиций × 4 радиуса + аналитический
  старт), чтобы не залипнуть в локальном минимуме `F`;
- **финальное доуточнение** из лучшего старта малым симплексом.

```python
from scipy.optimize import minimize

_SXY = [(0,0),(.7,.7),(-.7,.7),(-.7,-.7),(.7,-.7),(1.2,0),(0,1.2),(-1.2,0),(0,-1.2)]
_SR2 = (0.3, 0.5, 0.7, 0.9)

def solve_xyr2(A_meas, warm=None):
    """A_meas = [A1,A2,A3,A4] → (x, y, R2, F). warm — старт от прошлого кадра."""
    A_meas = np.asarray(A_meas, float)
    def F(p):
        x, y, R2 = p
        if not (0.02 < R2 <= 1.95):
            return 1e6
        return float(np.sum((forward_areas(x, y, R2) - A_meas) ** 2))

    A1, A2, A3, A4 = A_meas
    A_tot = float(A_meas.sum())
    p0 = [1.5*((A1+A4)-(A2+A3)), 1.5*((A1+A2)-(A3+A4)),
          min(0.95, max(0.05, 1.2*math.sqrt(A_tot/math.pi)))]
    starts = ([warm] if warm is not None else []) + [p0] + \
             [[sx, sy, sr] for sx, sy in _SXY for sr in _SR2]

    best = None
    for s in starts:
        r = minimize(F, s, method="Nelder-Mead",
                     options={"xatol": 1e-6, "fatol": 1e-12, "maxiter": 2000})
        if best is None or r.fun < best.fun:
            best = r
        if best.fun < 1e-12:                # глобальный минимум найден
            break
    # финальное доуточнение малым симплексом
    best = minimize(F, best.x, method="Nelder-Mead",
                    options={"xatol": 1e-6, "fatol": 1e-12, "maxiter": 2000})
    x, y, R2 = best.x
    return x, y, R2, float(best.fun)
```

**4. Условия применимости / достоверность** (стр. 5):

| Зона `d` (при `R1=1`) | Режим | Что определяется |
|---|---|---|
| `0 – 0.35`   | полное вложение | `R2` отлично, `(x,y)` вырождено (симметрия) |
| `0.35 – 1.20`| **оптимальная** | всё определяется, `κ ≈ 3–90` |
| `1.20 – 1.65`| краевая | малое перекрытие, чувствительна к шуму |
| `d > 1.65`   | нет пересечения | задача неприменима |

Нужно **`nz ≥ 2`** засвеченных квадрантов. При `nz = 1` (один квадрант) или при
`dx = dy = 0` задача вырождена: радиус `R2` в центре определяется отлично, но
`(x, y)` — нет (дуга решений). Признак ненадёжной оценки — остаток `F > 1e-3`.

**5. Перевод радиуса в пиксели.** Решатель даёт `R2` в единицах `R1 = 1`. Детектор
`R1 = 1` соответствует половине окна `size/2` (там же, где живёт `x_norm/y_norm`),
поэтому `r_px = R2 · size/2` (клампить под размер окна). Центр `(x, y)` — **побочный
продукт того же фита**: геометрически обоснованная замена текущей оценке направления
из `light_direction_to_point` (`point.x = x·size/2`, `point.y = y·size/2`).

### Практические замечания для проекта

- **Производительность.** Один полный решатель ≈ 76 000 вычислений прямой задачи
  (38 стартов × ~2000 итераций), каждое — интегрирование по `N = 500` полос. В C
  (PDF ссылается на `estimate_r2.c`) это реально в реальном времени; на чистом
  Python для потока нужно: векторизовать `forward_areas` через numpy (заменить цикл
  по `k` массивными операциями), снизить `N` и число стартов и применять **«тёплый
  старт»** от решения предыдущего кадра (`warm=` в `solve_xyr2`). В потоке пятно
  меняется плавно — обычно хватает 1 старта от прошлых `(x, y, R2)`, полный
  многостарт включать только при срыве (`F` подскочил).
- **Не использовать `√(f/π)` как ответ** — это только `R2_0`. Доли `b_i` из
  `spot_radius_px` переиспользуются как вход `A_i`, но итоговый радиус — из фита.
- **Параметры в `config.py`** (новый блок, ср. таблицу PDF, стр. 12):
  `N_INT = 500`, `NM_MAXITER = 2000`, `NM_FATOL = 1e-12`, `R2_CLIP = (0.02, 1.95)`,
  `F_RELIABLE = 1e-3` (если остаток выше — пометить кадр как ненадёжный).

### Минимальный план внедрения

1. Новый модуль `src/pipeline/spot_geometry.py`: `forward_areas(x, y, R2)` и
   `solve_xyr2(A_meas, warm=None) -> (x, y, R2, F)` (код выше).
2. В `make_point`: посчитать `b_i` → `A_i` (с раскладкой `ph ↔ s`), вызвать
   `solve_xyr2`, заполнить `Frame.radius = R2 · size/2` (и опц. `Frame.point` из
   `(x, y)`). Хранить тёплый старт между кадрами.
3. Параметры в `config.py::SensorConfig`/`DisplayConfig` (см. выше).
4. Отрисовка окружности уже есть (`display.py`, `spot_circle`) — менять не нужно.
5. Достоверность: при `F > F_RELIABLE` или `nz < 2` круг не рисовать (или рисовать
   серым) — индикатор ненадёжной оценки.

---
