# Алгоритм получения радиуса засвечиваемого пятна

Описание того, **как радиус пятна считается в коде** (а не общая теория).
Источник модели — `DOCUMENTATION/r2_estimation_algorithm.pdf`. Реализация:

| Что | Где |
|---|---|
| Прямая задача (площади по геометрии) | `src/pipeline/spot_geometry.py::forward_areas` |
| Обратная задача (фит x, y, R2)        | `src/pipeline/spot_geometry.py::solve_xyr2` |
| Доли засветки квадрантов              | `src/pipeline/get_single_point.py::quadrant_fracs` |
| Сборка кадра, радиус в пиксели        | `src/pipeline/get_single_point.py::make_point` |
| Фолбэк при вырождении                 | `src/pipeline/get_single_point.py::spot_radius_px_fallback` |
| Параметры решателя                    | `config.py::SensorConfig` |
| Отрисовка круга                       | `src/visualization/display.py` |

---

## 1. Идея

Детектор моделируется как **круг** `C1` радиуса `R1 = 1`, разбитый осями на 4
квадранта. Пятно засветки — **круг** `C2` с центром `(x, y)` и **неизвестным
радиусом `R2`**. По 4 измеренным площадям пересечения пятна с квадрантами
`A1..A4` совместно ищутся `(x, y, R2)` (переопределённая система: 4 уравнения,
3 неизвестных, решается МНК).

Радиус **нельзя** получить из суммарной площади отдельно: при смещении пятна
часть его уходит за край детектора и `A_tot < π·R2²`, поэтому `√(A_tot/π)`
занижает радиус тем сильнее, чем дальше пятно от центра. `R2` определяется только
совместным фитом всех 4 площадей — изменение `R2` меняет и суммарную площадь, и её
распределение по квадрантам.

Раскладка `ph ↔ s` (load-bearing, см. `CLAUDE.md`): квадранты PDF и датчики
связаны как `Q1(верх-право)=s4`, `Q2(верх-лево)=s1`, `Q3(низ-лево)=s2`,
`Q4(низ-право)=s3`.

---

## 2. Поток данных (один кадр в `make_point`)

На вход кадр-словарь `{s1, s2, s3, s4[, v_x, v_y, T]}` с сырыми значениями АЦП.

### Шаг 1. Доли засветки квадрантов — `quadrant_fracs`

Сырое значение переводится в долю засветки `b ∈ [0, 1]` по предельным значениям
квадранта `S_VAL_MAX`/`S_VAL_MIN` из `config.py`:

```
b(s) = clip( (S_VAL_MIN − s) / (S_VAL_MIN − S_VAL_MAX), 0, 1 )
```

При текущем конфиге `S_VAL_MAX = 0`, `S_VAL_MIN = 3500`:
`b = clip((3500 − s) / 3500, 0, 1)` — `s = 0` → `b = 1` (полная засветка),
`s ≥ 3500` → `b = 0` (нет засветки).

### Шаг 2. Сколько квадрантов засвечено — `nz`

```
nz = число квадрантов с b > cfg.FRAC_EPS      (FRAC_EPS = 0.01)
```

Дальше две ветки.

### Шаг 3а. `nz ≥ 2` — геометрический решатель (основной путь)

Из долей строятся 4 площади (полный квадрант = четверть круга = `π/4`), с
раскладкой `ph ↔ s`:

```
A_meas = ( (π/4)·b(s4),   # Q1 верх-право
           (π/4)·b(s1),   # Q2 верх-лево
           (π/4)·b(s2),   # Q3 низ-лево
           (π/4)·b(s3) )  # Q4 низ-право
```

Затем `solve_xyr2(A_meas, warm)` → `(x, y, R2, F)` (см. §3). Результат:

```
radius_px = min( R2 · (size/2),  0.95 · size/2 )      # R1=1 ↔ половина окна
reliable  = (F ≤ cfg.F_RELIABLE)
warm      = (x, y, R2)                                # тёплый старт для след. кадра
```

### Шаг 3б. `nz < 2` — фолбэк `spot_radius_px_fallback`

Геометрический фит вырожден (< 2 квадрантов). Тогда грубая оценка по сумме
(top-hat: равномерная интенсивность, всё пятно на детекторе):

```
f = mean(b_i);   radius_px = min( size·√(f/π),  0.95·size/2 )      # None при f=0
reliable = False                                                   # помечаем серым
warm     = None                                                    # сброс старта
```

### Шаг 3в. Нет сигнала

Если суммарная яркость `Σ max(0, ADC_MAX − s) ≤ 0` (все датчики ≥ `ADC_MAX`,
устройство шлёт 4096) — `radius = None`, точка в центре, `no_signal = True`,
`warm = None`.

На выходе — `Frame(point, v_x, v_y, radius=radius_px, spot_reliable=reliable, ...)`.

---

## 3. Решатель `solve_xyr2`

### 3.1. Прямая задача `forward_areas(x, y, R2)`

Площади пересечения круга `(x, y, R2)` с квадрантами круга `R1 = 1` — численным
интегрированием по горизонтальным полосам (метод средних прямоугольников,
`N = cfg.N_INT = 500`, правило средней точки убирает ложную асимметрию на границе
`y = 0`). Векторизовано через numpy. Для каждой полосы на высоте `yy`:

```
aa = √(R1² − yy²)                 # полуширина детектора C1
b  = √(R2² − (yy − y)²)           # полуширина пятна C2  (если выражение > 0)
xL = max(x − b, −aa);  xR = min(x + b, aa)
вклад правой части (xR>0) → Q1 если yy≥0 иначе Q4
вклад левой части  (xL<0) → Q2 если yy≥0 иначе Q3
```

Возврат: `[A_Q1, A_Q2, A_Q3, A_Q4]` = `[верх-право, верх-лево, низ-лево, низ-право]`.

### 3.2. Целевая функция

```
F(x, y, R2) = Σ_i [ forward_areas(x, y, R2)_i − A_meas_i ]²
```

с клампом `R2` в физический диапазон `cfg.R2_CLIP = (0.02, 1.95]` (вне диапазона
`F = 1e6`). Минимизируется методом **Нелдера–Мида** (`scipy.optimize.minimize`,
`method="Nelder-Mead"`, `fatol = cfg.NM_FATOL = 1e-12`, `maxiter =
cfg.NM_MAXITER = 2000`).

### 3.3. Стратегия запуска

1. **Тёплый старт (быстрый путь для потока).** Если передан `warm` (решение
   прошлого кадра) — один запуск NM от него. Если остаток `< cfg.F_RELIABLE`,
   сразу возвращаем результат (пятно меняется плавно — многостарт не нужен).
2. **Аналитическое приближение** (если тёплый старт не подошёл / отсутствует):
   ```
   x0   = 1.5·((A1+A4) − (A2+A3))      # право − лево
   y0   = 1.5·((A1+A2) − (A3+A4))      # верх − низ
   R2_0 = clamp(1.2·√(A_tot/π), 0.05, 0.95)
   ```
3. **Многостарт**: `x0`-старт + сетка `_SXY` (9 позиций) × `_SR2` (4 радиуса)
   = 37 стартов; обрывается, как только `F < NM_FATOL`.
4. **Финальное доуточнение** малым симплексом из лучшего старта.

Возврат `(x, y, R2, F)`. `R2` — радиус пятна в единицах `R1 = 1`; `F` — остаток
(мера достоверности).

---

## 4. Достоверность и визуализация

- `reliable = (F ≤ cfg.F_RELIABLE)`, где `F_RELIABLE = 1e-3`. Несошедшийся фит
  (`F` большой) или ветка фолбэка (`nz < 2`) → `spot_reliable = False`.
- `display.py` рисует круг радиуса `Frame.radius` вокруг точки:
  **жёлтый** при `spot_reliable = True`, **серый** при `False` (индикатор
  ненадёжной оценки). `radius = None` → круг не рисуется.

---

## 5. Параметры (`config.py::SensorConfig`)

| Параметр | Значение | Смысл |
|---|---|---|
| `S_VAL_MAX` / `S_VAL_MIN` | `0` / `3500` | пределы сырого значения квадранта (`b = 1` / `b = 0`) |
| `FRAC_EPS`  | `0.01` | порог «квадрант засвечен» для `nz` |
| `N_INT`     | `500`  | число полос интегрирования в `forward_areas` |
| `NM_MAXITER`| `2000` | макс. итераций Нелдера–Мида на старт |
| `NM_FATOL`  | `1e-12`| порог сходимости симплекса по `F` |
| `R2_CLIP`   | `(0.02, 1.95]` | физически допустимый диапазон `R2` |
| `F_RELIABLE`| `1e-3` | порог достоверности (выше → круг серый) |

---

## 6. Допущения и границы применимости

- **Top-hat**: интенсивность пятна по площади равномерна — площадь пересечения
  пропорциональна сигналу квадранта.
- **`nz ≥ 2`**: радиус определяется только при ≥ 2 засвеченных квадрантах; иначе
  фолбэк (занижает) и пометка «ненадёжно».
- **Зона перекрытия**: лучшая точность в зоне частичного перекрытия пятна и
  детектора. В режиме полного вложения `R2` определяется хорошо, но центр `(x,y)`
  вырождается; при сильном смещении (малое перекрытие) растёт чувствительность к
  шуму — такие кадры выявляются по росту `F`.
- **Поток**: тёплый старт держит цену ~1 запуск NM на кадр; первый кадр (или срыв
  фита) — полный многостарт.

---

# Задача №3: можно ли получить размер пятна из S₁..S₄?

## 3.0. Краткий вывод

Предположение «по `S_i` нельзя рассчитать радиус пятна» — **в основном верно** для
*абсолютного физического* размера: квадрантный детектор (QPD) — датчик
**положения**, а не **размера**. Но это **не абсолют**: размер становится
*частично* извлекаем, когда пятно взаимодействует с **геометрией детектора**
(границы квадрантов, внешний край) — это и эксплуатирует `spot_geometry.py`, но
ценой сильных допущений и лишь как *относительная модельная* оценка. Настоящий
физический размер задаётся **оптикой** и считается из FOC/фокуса либо измеряется
**нож-экспериментом** с известным смещением. Разбор ниже.

## 3.1. Что физически измеряет S_i

`S_i` — интеграл облучённости (мощности) по площади квадранта:

```
S_i ∝ ∫_{Q_i} I(x, y) dx dy        # это МОЩНОСТЬ (энергия луча), не геометрия
```

4 квадранта → 4 числа. Из них независимы по сути **3** величины:

```
P    = S1+S2+S3+S4                  # суммарная мощность
x0  ~ (S_право − S_лево) / P        # горизонтальный центроид
y0  ~ (S_верх  − S_низ ) / P        # вертикальный центроид
```

4-я комбинация (диагональ `S1−S2+S3−S4`) для симметричного пятна — почти шум.

## 3.2. Почему размер не выделяется (две независимые причины)

**(а) Размер и положение СЦЕПЛЕНЫ.** Нормированный разностный сигнал зависит от
**отношения** `x0/w`, а не от `x0` и `w` по отдельности. Для гауссова пятна:

```
(S_право − S_лево) / P = erf(√2·x0 / w) = f(x0/w)
```

Сдвиг пятна и изменение его размера дают одинаковый эффект — по одному кадру их
не разделить. У пятна ≥ 4 параметров (`P, x0, y0, w`), наблюдаемо 3 → система
**недоопределена** по `w`.

**(б) Мощность ≠ площадь (сцепка яркость↔размер).** `S_i` — мощность. Чтобы
получить из неё *площадь засветки*, нужно допущение о **фиксированной известной
интенсивности**. Именно это делает текущий код
(`get_single_point.py::quadrant_fracs` + `make_point`):

```
b_i = clip((S_VAL_MIN − s_i)/(S_VAL_MIN − S_VAL_MAX), 0, 1)
A_i = (π/4)·b_i        # доля СИГНАЛА приравнена к доле ПЛОЩАДИ квадранта
```

Это верно только при равномерной (top-hat) и калиброванной интенсивности. Иначе
**малое яркое** пятно и **большое тусклое** дают одинаковые `S_i` → алгоритм
спутает яркость с размером. В реальном потоке (меняющаяся яркость источника)
оценка `R2` оказывается **смешана с интенсивностью**.

## 3.3. Когда размер ВСЁ-ТАКИ извлекаем (как это делает spot_geometry.py)

Единственная «линейка» у QPD — его **границы**: крест между квадрантами и внешний
край круга `R1`. Когда пятно **частично перекрывает край / сидит на границе**,
перераспределение мощности по квадрантам становится чувствительно к `R2`
**отдельно** от `(x, y)` (якобиан `∂A/∂R2` линейно независим — ранг 3). Это и
эксплуатирует `solve_xyr2`.

Цена — допущения: (1) top-hat-интенсивность, (2) известная калибровка
`S_VAL_MAX/S_VAL_MIN`, (3) пятно в зоне частичного перекрытия (в режиме полного
вложения размер вырожден). ⇒ полученный `R2` — **эффективный, модельный,
относительный** индикатор, а не измерение физического размера.

## 3.4. Итог анализа предположения

| Что хотим из одного кадра S_i | Возможно? |
|---|---|
| Положение (центроид x0, y0)        | ✅ да — это и есть назначение QPD |
| Суммарная мощность P               | ✅ да |
| **Абсолютный физический радиус w** | ❌ нет — недоопределён, сцеплен с x0/w и с яркостью |
| Эффективный top-hat-радиус R2      | ⚠️ только в зоне края и при фикс. калибровке яркости |

**Предположение верно** для абсолютного размера. QPD меряет *куда* светит, а не
*насколько широко*.

## 3.5. Откуда брать настоящий размер: ОПТИКА (FOC / фокус)

Физический размер пятна задаётся **оптической системой**, а не фотодиодами, и его
можно **вычислить** из параметров оптики:

```
w ≈ 1.22·λ·f / D            (дифракционный предел, в фокусе)
       ⊕ D·|Δz| / (2·f)     (геометрический дефокус, если детектор не в фокусе)
       ⊕ аберрации
```

где `λ` — длина волны источника, `f` — фокусное расстояние оптики (введённый
параметр **FOC**), `D` — диаметр апертуры, `Δz` — дефокус (смещение детектора от
фокальной плоскости). Поле зрения и координата связаны `x = f·tan(θ)` (угол
источника → точка на детекторе).

Если детектор стоит в фокальной плоскости (`Δz ≈ 0`), пятно
**дифракционно-ограничено**: `w ≈ 1.22·λ·f/D` — **фиксированная константа**, не
зависящая от живого сигнала.

**Метод расчёта размера из FOC:** подставить `λ, f, D, Δz` в формулу выше — это
design-time константа. В потоке тогда рисуем **фиксированный** (или зависящий от
дефокуса) радиус, а не выводим его из яркости.

## 3.6. Физический эксперимент для измерения размера

**A. Нож-сканирование по границе квадранта (рекомендуется).** Готовые «ножи» —
сами границы квадрантов. Двигая источник/датчик на **известный** шаг `Δx`
(микрометрический столик или калиброванный угловой свип) и записывая
`(S_право − S_лево)/P` против `Δx`, получаем erf-отклик; его наклон в центре даёт
размер:

```
d/dx[(S_право − S_лево)/P]|₀ = 2/(√(2π)·w)   ⇒   w = √(2/π) / наклон
```

**Известное смещение `Δx`** — та самая недостающая независимая переменная,
которой нет в одном кадре. (Связь с исходной идеей TASK «провести датчиком из
стороны в сторону»: свип = нож-эксперимент; но нужно фиксировать смещение.
Авто-обновление min/max из потока калибрует только **яркость** `S_VAL`, не размер.)

**B. Прямое профилирование (камера/beam-profiler).** В плоскости детектора —
калиброванная камера; диаметр меряется напрямую (D4σ / второй момент, ISO 11146).
Эталон, но нужна камера.

**C. Пинхол-сканирование.** Малое отверстие сканирует пятно; по карте мощности —
фит профиля и размера.

**D. Дефокус-серия.** Сдвигать детектор вдоль оси на известный `Δz`;
геометрический радиус растёт линейно `r(Δz) = r0 + (D/2f)·|Δz|`. Наклон → `D/f`,
минимум `r0` → дифракционный предел. Разделяет оптические параметры
экспериментально.

**E. Валидация алгоритма (трансляционный столик).** Двигать источник на известный
`Δ`. Проверить: `x` из `solve_xyr2` линеен по `Δ` (калибровка px↔мм + проверка
центроида), а `R2` **не должен** меняться при сдвиге (размер от положения не
зависит). Дрейф `R2` обнажает сцепку яркость/размер из §3.2(б).

## 3.7. Рекомендация

1. Размер пятна — **константа оптической системы**: посчитать `w = 1.22·λ·f/D`
   (+ дефокус) из `λ, f (FOC), D`.
2. Подтвердить/откалибровать **нож-сканированием по границе** с известным
   смещением (эксперимент A) — даёт `w` в единицах детектора и связь px↔угол.
3. В потоке рисовать фиксированный радиус из (1)–(2); текущий `solve_xyr2`
   оставить как **относительный live-индикатор** «дыхания» пятна в зоне края,
   честно помечая ненадёжные кадры серым (уже реализовано).

---

# Задача №4: калибровка нож-сканированием — анализ

Процедура: 3 фиксации (центр `i0`, крайнее правое `i1`, крайнее левое `i2`),
на каждой пользователь выставляет поворотный столик и вводит угол; получаем
`{"i0": 0.0, "i1": 2.0, "i2": -2.0}` → JSON.

## 4.0. Прямой ответ: ДА, со смещением размер получается

**Известное смещение — это ровно та недостающая независимая переменная, которой
не было в Задаче №3.** В одном кадре размер и положение сцеплены (разностный
сигнал зависит только от отношения `x/w`). Как только смещение `x` **задано
извне** (углом столика), отношение `x/w` перестаёт быть неизвестным целиком —
остаётся одна неизвестная `w`, и она **однозначно определяется**.

## 4.1. Почему так (ключевая физика)

Нормированный разностный сигнал по вертикальной границе квадрантов:

```
D = (S_право − S_лево) / P ,   P = S1+S2+S3+S4
```

Для **любого** симметричного профиля пятна `D` зависит только от **отношения**
`x/w` (смещение / размер):

```
D = h(x / w)          # гаусс: h = erf;  top-hat-диск: h = g (через площадь сегмента)
```

Поэтому при **известном** `x` инверсия даёт размер:

```
w = x / h⁻¹(D)
```

Смещение на детекторе из угла столика и фокуса (FOC): `x = f·tan(θ) ≈ f·θ`.

## 4.2. Формула размера из 3 точек

- `i0` (θ₀): задаёт нуль `D≈0` и ловит механическое смещение центра.
- `i1, i2` (±θ): дают `|D|` при известном `x₁ = f·tan(θ₁)`.

**Гаусс** (`w` — радиус по уровню 1/e², профиль `I∝exp(−2x²/w²)`):

```
D₁ = erf(√2·x₁ / w)   ⇒   w = √2·x₁ / erfinv(D₁)
```

**Top-hat-диск** (модель `spot_geometry`): `D` не имеет закрытой erf, инверсия
численная — фактически это `solve_xyr2` с **зафиксированным** `x` (см. §4.5).

Два симметричных плеча (`i1`,`i2`) → усреднение `|D|` (шум ↓) и проверка
симметрии `D(+θ)≈−D(−θ)` (контроль перекоса).

## 4.3. Что ОБЯЗАТЕЛЬНО добавить к процедуре

1. **Писать не только углы, но и сигнал.** Угол сам по себе размер не даёт — на
   каждой точке нужно сохранить усреднённые `S1..S4` (⇒ `D_i`, `P_i`). Без них в
   JSON нечего обрабатывать. → расширить JSON (см. §4.7).
2. **Нужен `f` (FOC) или масштаб «угол↔детектор».** Чтобы `w` получить в пикселях
   / `R1=1`, угол переводят в смещение `x=f·tanθ`. Без `f` калибровка даёт `w`
   только в **угловых** единицах (что тоже полезно). Масштаб можно и
   само-откалибровать (§4.4, п.4).
3. **Усреднять кадры на «зелёной» фазе.** Когда позиция зафиксирована — собрать
   N кадров и усреднить `S_i` (и записать std как меру качества).

## 4.4. Что стоит улучшить

1. **Рабочая точка ±θ.** Выбирать угол так, чтобы `|D|` попадал в середину
   диапазона (~0.3…0.8): при `D→0` инверсия тонет в шуме (0/0), при `D→±1` —
   насыщение и `erfinv→∞`. «Крайние» право/лево не должны уводить пятно в полное
   насыщение или за край детектора. Можно сделать угол **адаптивным** (увеличивать,
   пока `|D|` не достигнет ~0.5).
2. **Только горизонтальная ширина.** Лево-право зондирует вертикальную границу →
   даёт `w_x`. Для 2D-размера добавить скан верх/низ (`i3,i4`) → `w_y`. Если пятно
   считаем круглым (модель `R2`), хватает одной оси: `w=w_x`.
3. **Профиль определяет константу.** `w=√2·x₁/erfinv(D₁)` — для гаусса (1/e²).
   Для диска — численная инверсия. Структура `w∝x₁` одинакова; результат в тех же
   единицах, что и `x₁`. Указывать принятую модель профиля в JSON.
4. **Само-калибровка масштаба (бонус).** По 3 точкам фитим `D_i = erf(a·(θ_i−θ₀))`
   → получаем наклон `a` и истинный центр `θ₀`; угловой размер `w_θ = √2/a`. Тогда
   отдельный `f` не нужен — метрику задаёт сам столик (3 значения `D`, 2–3
   неизвестных — определимо).

## 4.5. Связь с уже реализованным кодом

Калибровочную точку можно решать **тем же** `solve_xyr2`/`forward_areas`, но с
**зафиксированными** `x=x₁` (из угла), `y=0`: остаётся 1 неизвестная `R2` →
одномерная задача, вырождение Задачи №3 снято, `R2` определяется точно. То есть
Задача №4 — это «Задача №2 с известным центром».

## 4.6. Замечания по самой процедуре

- **Критерий «зелёного».** Для центра — авто: `|D_x|,|D_y| < ε`. Для право/лево
  позицию задаёт пользователь столиком, поэтому проще ручное подтверждение
  (клавиша) + ввод угла в терминале; зелёный = «позиция принята, идёт усреднение».
- **Метка `сканирование i`** и смена цвета точки на зелёный — ок как UX; добавить
  показ текущего `D` и `P`, чтобы видеть, что точка не в насыщении.

## 4.7. Расширенный формат JSON (предложение)

```json
{
  "profile": "gauss_1e2",
  "focal_fov": 12.0,
  "points": {
    "i0": {"angle": 0.0,  "s": [s1,s2,s3,s4], "D": 0.00, "P": 1234, "n": 30, "std": 4},
    "i1": {"angle": 2.0,  "s": [s1,s2,s3,s4], "D": 0.52, "P": 1230, "n": 30, "std": 5},
    "i2": {"angle": -2.0, "s": [s1,s2,s3,s4], "D": -0.50,"P": 1228, "n": 30, "std": 5}
  },
  "result": {"w_angular": 1.93, "theta0": 0.03, "fit_residual": 1e-3}
}
```

## 4.8. Итог

Да: три фиксации (центр + ±θ) с **записанным сигналом** дают размер пятна через
`w = x₁/h⁻¹(D₁)`, `x₁=f·tan(θ₁)`. Минимум (3 точки) — точно определённая система
(центр + ширина); больше точек/полный свип — устойчивее. Обязательно: сохранять
`S_i` на каждой точке и знать (или само-откалибровать) масштаб угол↔детектор.

---

# Проверка расчёта радиуса пятна (верификация, 2026-06-08)

Прогон **фактического кода** обоих путей расчёта радиуса. Конфиг на момент
проверки: `FOC = 60` мм, `DET_SIZE_MM = 14.0`, `SIZE_DISPLAY = 200`,
`CALIB_PX_PER_MM = SIZE_DISPLAY/DET_SIZE_MM = 14.2857` px/мм.

## V.1. Путь калибровки — `calib_radius.py::spot_radius_from_calib`

Постоянный радиус из `DATA/CALIB/CALIBRATE.json` по нож-модели
`w = √2·x/erfinv(D)`, `x = FOC·tan θ`. Результат:

| точка | D (с коррекцией центра) | θ | w |
|---|---|---|---|
| i1 | +0.488 | +2.5° | 7.99 мм |
| i2 | −0.483 | −2.5° | 8.08 мм |

→ **w ≈ 8.03 мм**, `radius_px = 114.8`.

**Вывод: математика корректна и внутренне согласована.** Два симметричных плеча
дают `w` с расхождением ~1 % — сильный признак, что модель и данные когерентны.
Раскрутка угла (`357.5°→−2.5°`), коррекция нуля по `i0` (`d0`) и независимость от
знака (`abs(D)`, `abs θ`) — отрабатывают верно.

Замечания (не ошибки математики, но требуют решения):

1. **Нет клампа → круг выходит за окно.** `radius_px = 114.8` при полуразмере
   дисплея `100` (видимая зона `±105`). Пятно физически **больше детектора**
   (`w = 8 мм` > радиус детектора `7 мм`), поэтому окружность рисуется крупнее
   кадра и обрезается. Числовое значение `w_mm` при этом верное.
2. **`fov` берётся из `cfg.FOC`, а не из JSON.** `save_calibration` пишет `fov` в
   файл, но `spot_radius_from_calib` его не читает. Текущий `CALIBRATE.json`
   поля `fov` не содержит (старый файл), поэтому работает на `cfg.FOC`; но `w`
   линейно зависит от `cfg.FOC` → смена конфига молча пере-масштабирует старые
   калибровки. Рекомендация: читать `fov` из JSON с откатом на `cfg.FOC`.
3. **Устаревший комментарий** `# м` у `fov` (фактически мм). Косметика.

## V.2. Потоковый путь — `spot_geometry.py::solve_xyr2`

**Решатель корректен в своей рабочей зоне.** Round-trip
(`forward_areas → solve_xyr2`) точен, пока пятно помещается в детектор:

```
R2=0.60            -> 0.6000   (F=1.6e-13)
R2=0.80 (x+0.3)    -> 0.8000   (F=1.9e-13)
R2=0.90 (x−0.4)    -> 0.9000   (F=5.3e-15)
R2=0.50 (x+0.6)    -> 0.5135   (dR2=0.014 — слабая неоднозначность при смещении)
R2=1.20 центр      -> 1.0028   (dR2=0.197 — НАСЫЩЕНИЕ)
```

На реальном `putty.csv` (с `fixed_radius=None`): 340/352 кадров получают радиус,
**4.0–5.0 мм** (median 4.83), 331/340 надёжны, **0 кадров за пределами окна**.
Масштаб согласован с калибровкой: `r_mm = radius_px·DET_SIZE_MM/size = R2·7`.

**Фундаментальное ограничение (насыщение).** Когда пятно полностью накрывает
детектор (`R2 ≥ 1`, центр), все 4 площади равны `π/4` независимо от истинного
`R2` → радиус **невосстановим**, решатель упирается в `R2 ≈ 1.0` (~7 мм) с
`F ≈ 0`, то есть помечает кадр как **надёжный**. На текущих данных не возникает
(пятна 4–5 мм), но флаг достоверности этот случай **не ловит**.

## V.3. Согласованность масштаба обоих путей

`radius_mm = radius_px · DET_SIZE_MM/size` — то же преобразование, которым
калибровка переводит мм→px. Поэтому оба пути на **одной мм-шкале**: калибровка
показывает `r ≈ 8.03 мм (57 % зоны 14 мм)`, поток — `~4–5 мм`. ✅

## V.4. Итог проверки

| Путь | Математика | На реальных данных | Замечания |
|---|---|---|---|
| Калибровка (константа) | ✅ верна, плечи сходятся (7.99 / 8.08 мм) | `w≈8.03 мм`, `r=114.8 px` | круг за окном (нет клампа); `fov` из cfg, не из JSON |
| Поток (`solve_xyr2`) | ✅ верна в зоне `R2≲1`; насыщение при `R2≥1` | 4–5 мм, 331/340 надёжны, без переполнения | насыщение не помечается как ненадёжное |

---

# Углы отклонения центра — `angle_x`, `angle_y`

Углы отклонения центра луча от оптической оси по горизонтали и вертикали,
показываются на дисплее (`θx`, `θy`). Реализация:
`src/pipeline/get_single_point.py::deflection_angles`, поля `Frame.angle_x/​angle_y`,
отрисовка — `src/visualization/display.py`.

## A.1. Идея

Угол требует **смещения центра пятна** на детекторе, а оно (по нож-модели)
выражается через измеренный разностный сигнал `D` **и радиус пятна `w`** — отсюда
зависимость угла «от фокуса и радиуса пятна». Это та же нож-инверсия, что в
калибровке (§4.1), но прогоняется **в каждом кадре** для текущего `D`.

## A.2. Формулы

Нормированные разностные сигналы (как в `light_direction_to_point`):

```
D_x = x_norm = (b4 + b3 − b1 − b2) / Σb        # право − лево
D_y = y_norm = (b1 + b4 − b2 − b3) / Σb        # верх − низ
```

Радиус пятна в мм (та же шкала, что в §V.3):

```
w_mm = radius_px · DET_SIZE_MM / size
```

`radius_px` — постоянный из калибровки **или** покадровый из `solve_xyr2`/фолбэка:
формула едина для обоих путей. Далее нож-инверсия и угол:

```
d = w_mm · erfinv(D) / √2          # смещение центра на детекторе, мм
θ = atan( d / FOC )                # угол отклонения от оси, рад → градусы
```

по каждой оси отдельно: `θx = θ(D_x)`, `θy = θ(D_y)`. Для устойчивости `|D|`
клампится к `≤ 0.999` (`erfinv(±1) → ∞`). Если радиуса/фокуса нет (`w_mm ≤ 0`
или `FOC ≤ 0`) — `angle_x = angle_y = None` (на дисплее не выводятся).

## A.3. Почему `θ` зависит и от FOC, и от `w`

`D = erf(√2·d/w)` связывает **отношение** `d/w` с сигналом. Чтобы из `D` получить
само смещение `d` (а не отношение), нужен **`w`** — иначе `d` неопределимо
(сцепка размер↔положение из §3.2а). А перевод смещения `d` в угол требует
**фокуса**: `θ = atan(d/FOC)`. Поэтому в расчёт входят обе величины — ровно как
в постановке задачи.

## A.4. Проверка (2026-06-08)

- **Юнит-тест.** `deflection_angles` совпадает с `atan(w·erfinv(D)/√2 / FOC)`:
  `D=0.5 → 2.575°`, `D=0.9 → 6.258°`; `θy=0` при `D_y=0`; `w≤0 → (None, None)`.
- **End-to-end на `putty.csv`** (константный радиус калибровки): все 340 кадров с
  сигналом получают углы, `θx ∈ [−1.43°, 3.38°]`, `θy ∈ [−1.62°, 1.55°]`; кадры
  «нет сигнала» сохраняют `None`.
- **Сквозная согласованность с калибровкой.** Калибровка снята при `θ = 2.5°`
  (там `D = 0.488`). Обратный прогон `deflection_angles(0.488, …, w=8.03, FOC=60)`
  даёт `2.51°` — то есть формула углов является точной **инверсией**
  калибровочного соотношения и восстанавливает угол столика. ✅

## A.5. Замечание

`θ` наследует масштаб радиуса: при `w ≈ 8 мм` (см. §V.1) углы считаются от этого
`w`. Косметический кламп круга на дисплее (замечание §V.1.1) на **значения углов
не влияет** — они используют истинный `w_mm`, а не обрезанный пиксельный радиус.

---

# Каких параметров не хватает для расчёта V_x/V_y по статье

Источник: `DOCUMENTATION/Design_and_analysis_of_optical_system.pdf` (Pu, Du, Dong,
2020), формулы (1)/(2). Разбор: что требует формула статьи и чего для этого нет
(или задано наугад) в проекте.

## S.0. Формула статьи и её переменные

```
V_x = (2·K_x / π) · [ (x/r)·√(1 − (x/r)²) + arcsin(x/r) ]            (1)
V_y = (2·K_y / π) · [ (y/r)·√(1 − (y/r)²) + arcsin(y/r) ]            (2)
```

- `x, y` — смещение центра пятна на детекторе, мм;
- `r` — радиус пятна, мм;
- `K_x, K_y` — усиление (gain).

Чтобы превратить сигнал в **угол**, статья отдельно использует формулу (4):
`x = f·tan θ` (`f` — фокус). То есть угол требует ещё и `f`.

## S.1. Ключевой момент: из сигнала восстановим только x/r

Правая часть (1) зависит **только от отношения `x/r`**. Нормированная разность
квадрантов, которую проект уже считает (`x_norm`, `y_norm ∈ [−1, 1]` в
`light_direction_to_point`/`make_point`), — это и есть левая часть с точностью до
`2K/π` (на `x=r` скобка `= π/2`, сигнал `= 1` — полная шкала).

⇒ из 4 показаний датчиков извлекается **только `x/r`**, а не сам угол. Чтобы
получить угол, нужно **независимо** знать `r` (мм) и `f` (мм); устройство их не
меряет. Это та же сцепка размер↔положение, что в §3.2(а).

## S.2. Сверка с проектом (уточнено по данным железа, 2026-06-08)

`cfg.FOC = 50 мм` — **реальное** фокусное расстояние; активная зона детектора
**14 мм** (`DET_SIZE_MM` верен). Значения `s1..s4` в логах — **оцифрованное
напряжение квадранта** (gain `K` уже применён в железе): `s ≈ 10..30` ↔ **2.5 В**
(есть засветка), `s ≈ 3500` ↔ **0 В** (нет засветки). Шкала инверсная: меньше код
→ больше напряжение.

| Переменная (1)/(2) | Для чего | Статус в проекте |
|---|---|---|
| `f` — фокус | угол `θ = atan(x/f)` | `cfg.FOC = 50` мм — **реальный фокус**. ✅ |
| `DET_SIZE_MM` (масштаб px↔мм) | `r` и `x` в мм | `= 14.0` — **реальный размер**. ✅ |
| `K_x, K_y` (усиление) | напряжение V в вольтах | **уже зашит в `s_i`** (оцифрованное напряжение); в нормированном отношении сокращается → отдельно не нужен |
| `r` — радиус пятна, мм | пересчёт `x/r → x` (мм) | оценивается (`solve_xyr2`/калибровка); единственное, что детектор не меряет напрямую |
| модель профиля пятна | инверсия сигнала → `x/r` | проект: гаусс-нож (`erf`/`erfinv`); статья: **равномерный диск** (`arcsin`-форма) |

## S.3. Что осталось (после уточнения — недостающих параметров нет)

С учётом реальных `f = 50 мм`, `det = 14 мм` и `K`, уже зашитого в `s_i`, для
**угла** в пайплайне есть всё. Открыты не «недостающие параметры», а **выборы
модели и точности**:

1. **Радиус пятна `r`.** Единственная величина, которую детектор не меряет
   напрямую. Проект её даёт: покадрово (`solve_xyr2`) или фиксированной из
   нож-калибровки (§4, §V.1). Точность угла наследует точность `r`.
2. **Модель инверсии.** Проект инвертирует через `erf` (гаусс-нож, §A.2); статья —
   через равномерный диск (`arcsin`, формула (1)). Для строгой согласованности со
   статьёй заменить инверсию на дисковую форму. На малых `x/r` обе близки.
3. **База яркости для направления (потенциальная неточность).** По калибровке 0 В
   (темно) = код `≈ 3500`, а `make_point` считает яркость как `max(0, ADC_MAX −
   s)` с `ADC_MAX = 4096`. Тёмный квадрант (`s ≈ 3500`) тогда даёт яркость `≈ 596`,
   а не 0. В числителе `x_norm` это смещение сокращается (баланс +/−), но
   **знаменатель `Σb` раздувается на `4·596 ≈ 2384`** → магнитуда `x_norm/y_norm`
   занижается, а с ней и угол (при одном засвеченном квадранте — десятки %). Для
   направления опорой яркости логичнее `≈ 3500` (`S_VAL_MIN`), оставив `4096`
   только как сигнатуру «нет сигнала» (все 4 квадранта). Квадрантный путь
   (`quadrant_fracs`) уже использует `3500` верно — затронут только путь
   направления.

## S.4. Если нужно само напряжение V_x/V_y (вольты)

Поквадрантное напряжение восстанавливается прямо из кода датчика:

```
V_i = 2.5 · (3500 − s_i) / (3500 − 20)        # ≈ (3500 − s_i)/1392, В
```

Дальше `V_x`/`V_y` — сумма/разность по статье (право−лево / верх−низ); для угла их
всё равно нормируют на `ΣV` (тогда `K` и интенсивность сокращаются — это ровно
`x_norm`/`y_norm` из `make_point`).

---

# Задача №14: определение углов методом Нелдера–Мида — анализ

Вопрос: можно ли определять углы отклонения по `s1..s4` методом оптимизации
Нелдера–Мида (NM)? Какие начальные данные нужны? Тесты — на точках
`MEASURE.json` с углами `−1.5…1.5°` (92 точки из 110).

## 14.0. Краткий вывод

**Да, но только с заранее известным размером пятна `w`.** NM минимизирует невязку
между прямой моделью «пятно на 4 квадрантах» и измеренными `s_i`, восстанавливая
центр `(x, y)` → угол `θ = atan(x/FOC)`. В тестовом диапазоне это **работает,
если `w` подан как вход** (из нож-калибровки), и тогда NM по точности **равен**
замкнутой нож-инверсии, которая уже есть в коде (`deflection_angles`), — то есть
ничего нового не даёт, только дороже и с риском локальных минимумов. **Без
известного `w` задача в центре вырождена** (наблюдаемо лишь отношение `x/w`),
и NM выдаёт угол, целиком зависящий от *предположенного* `w`. Сам метод NM в
проекте уже задействован — `spot_geometry.py::solve_xyr2` (там он оправдан, см.
§14.6).

## 14.1. Что значит «определить угол через NM»

Угол не наблюдается напрямую — наблюдаются 4 яркости `b_i = max(0, ADC_MAX − s_i)`.
Схема NM-инверсии:

```
параметры (x, y[, P, w])  ──forward──►  модельные b_i  ──┐
                                                         F = Σ(b_i^модель − b_i^изм)²  ──► min (NM)
измеренные s_i  ──► b_i  ──────────────────────────────┘
угол: θx = atan(x/FOC),  θy = atan(y/FOC)
```

Прямая модель (сепарабельный гаусс/нож; та же физика, что в `deflection_angles`):

```
Fx_право = (1 + erf(√2·x/w))/2,   Fy_верх = (1 + erf(√2·y/w))/2
b(верх-лево)=P·Fx_лево·Fy_верх,  b(верх-право)=P·Fx_право·Fy_верх,  и т.д.
⇒ Dx = (право−лево)/P = erf(√2·x/w),   Dy = erf(√2·y/w)
```

То есть NM решает ту же нож-модель, но численно и совместно по осям. (Можно взять
и непрерывную дисковую модель `solve_xyr2` — суть та же.)

## 14.2. Ключевое ограничение — вырождение «размер ↔ положение»

В диапазоне `−1.5…1.5°` пятно (`w ≈ 3.9 мм`) целиком внутри детектора (14 мм): при
`θ = 1.5°` смещение `x = FOC·tan θ ≈ 1.3 мм`, край пятна `x+w ≈ 5.2 мм < 7 мм` —
**обрезания о край нет**. Сигнал зависит только от **отношения** `x/w`
(`Dx = erf(√2·x/w)`), поэтому `x` и `w` по одному кадру **неразделимы** (та же
сцепка, что в §3.2а / Задача №3).

Эксперимент (точка `i71`, истинный угол −0.5°, NM по `(x, y, P, w)` из разных
стартовых `w`):

| старт `w₀`, мм | итог `w`, мм | `x`, мм | `θx` | остаток `F` |
|---|---|---|---|---|
| 2.0  | 1.99  | −0.20 | −0.23° | 1.96·10³ |
| 4.0  | 3.98  | −0.40 | −0.46° | 1.96·10³ |
| 8.0  | 7.97  | −0.81 | −0.93° | 1.96·10³ |
| 16.0 | 15.94 | −1.62 | −1.85° | 1.96·10³ |

Остаток `F` **одинаков** при всех `w`, а восстановленный угол гуляет от −0.23° до
−1.85° (`x ∝ w`, отношение `x/w` неизменно). Невязка плоская вдоль гребня `x/w` ⇒
**`w`, а с ним и абсолютный угол, не идентифицируемы** без внешнего знания `w`.

## 14.3. NM с известным `w` — работает, но равен нож-инверсии

Зафиксировав `w` (калибровка дала `wx=3.97`, `wy=3.86` → `w≈3.91 мм`) и решая NM
по `(x, y, P)` на всех 92 точках:

| метод (на 92 точках −1.5…1.5°) | RMSE θx | RMSE θy |
|---|---|---|
| NM по `(x,y,P)`, `w` известен | 0.080° | 0.114° |
| closed-form нож `θ=atan(w·erfinv(D)/√2/FOC)` | 0.091° | 0.102° |
| **компенсационный полином (паспорт)** | **0.025°** | **0.039°** |

NM воспроизводит нож-модель (различие — численный шум/локальные минимумы). И NM, и
нож **проигрывают** эмпирическому полиному, потому что идеальная гаусс-модель не
описывает реальный профиль и перекрёстную связь осей (см. анализ асимметрии в
`ANALYSE_POLINOME.md`), а полином подогнан под реальные данные и поглощает эту
систематику.

## 14.4. Какие начальные данные нужны для NM

1. **Прямая модель + профиль пятна** — сепарабельный гаусс/нож (`erf`) или
   дисковая модель `forward_areas` (`solve_xyr2`). Задаёт `(x,y,w[,P]) → s_i`.
2. **Размер пятна `w` — критический вход.** Без него центр вырожден (§14.2).
   Берётся из нож-калибровки (`CALIBRATE.json`, §4/§V.1) или из оптики
   (`w≈1.22·λ·f/D`+дефокус, §3.5). Точность угла наследует точность `w`.
3. **`FOC`** (`cfg.FOC = 50 мм`) — перевод центра `x,y` [мм] → угол `atan(x/FOC)`.
4. **Шкала яркости** `b = max(0, ADC_MAX − s)` (`ADC_MAX`) и **раскладка `ph↔s`**
   (load-bearing, см. CLAUDE.md) — иначе модель зеркалится.
5. **Полная мощность `P`** — как свободный масштаб (или нормировать, работая с
   долями/`D`).
6. **Стартовая точка симплекса** `(x₀, y₀)` — NM локальный, нужен разумный старт
   (линейный центроид `D` годится) + клампы `w`/`R2` (как `R2_CLIP` в `solve_xyr2`).
7. **Параметры сходимости** `fatol`, `xatol`, `maxiter` (как `NM_FATOL`,
   `NM_MAXITER`).

## 14.5. NM против текущих методов

| Критерий | NM (физ. модель) | closed-form нож | компенс. полином |
|---|---|---|---|
| нужен ли `w` | да (иначе вырождение) | да | нет (учится по `D`) |
| вычисления | итеративно, ~10²–10³ оценок/кадр | замкнутая формула | один `predict` |
| детерминизм | локальные минимумы | да | да |
| точность −1.5…1.5° | ~0.08–0.11° | ~0.09–0.10° | **~0.03–0.04°** |
| связь осей | если заложена в модель | нет (по осям) | да (2D-полином) |

В линейном диапазоне NM **не даёт выигрыша** — это более дорогой способ решить ту
же сепарабельную нож-задачу, и он всё равно требует `w`.

## 14.6. Где NM реально оправдан

- **Большие углы / взаимодействие с краем детектора.** Когда пятно частично
  выходит за край, перераспределение по квадрантам становится чувствительно к `w`
  **отдельно** от `(x,y)` (ранг 3) — вырождение §14.2 снимается, и NM по
  `(x,y,R2)` даёт и положение, и размер. Это ровно то, что делает
  `solve_xyr2` — и это **вне** тестового диапазона `−1.5…1.5°`.
- **Совместный 2D-фит**, если в прямую модель заложить перекос осей/профиль —
  «белый ящик» вместо эмпирического полинома. Но точность ограничена качеством
  модели и в диапазоне полином не побьёт.
- **3-квадрантный режим** (один квадрант тускнеет, см. `ANALYSE_POLINOME.md`): NM
  может использовать все 4 яркости совместно при известном `w`, чуть расширяя
  диапазон, — но наследует потерю информации у насыщения.

## 14.7. Рекомендация

1. Для углов в `−1.5…1.5°` **не использовать NM** — замкнутая нож-инверсия +
   компенсационный полином точнее, быстрее и детерминированы; NM лишь повторно
   решает то же и всё равно требует `w`.
2. Если нужна физическая (модельная) оценка угла без эмпирического полинома — NM
   по `(x,y)` с **фиксированным калиброванным `w`**: та же точность, что у ножа,
   ценой вычислений.
3. Единственная ниша, где NM выигрывает, — **совместная оценка положения и размера
   при работе пятна у края** (большие углы), что уже реализовано в `solve_xyr2`.

Параметры эксперимента: `FOC=50`, `ADC_MAX=3500`, `w≈3.91 мм` (калибровка по
данным), 92 точки из `MEASURE.json` с `|angle| ≤ 1.5°`.

---

# Разбор отчёта `cli_model.py fit --degree 3` (построчно)

Что печатает каждая строка вывода команды `python cli_model.py fit --degree 3`.
Код печати — `cli_model.py::_print_report` / `_print_verify`; математика —
`src/compensation.py`. Полином корректирует систематику нож-модели угла
(Задача №13). Данные взяты из очищенного `MEASURE_dump.json` (83 точки,
`|angle| ≤ 1°20'`).

## R.1. Блок «ПОСТРОЕНИЕ ПОЛИНОМА»

```
=== ПОСТРОЕНИЕ ПОЛИНОМА ===
степень 3 (10 коэф./ось), базис erfinv, d_max=0.95, ADC_MAX=3500
```
- **степень 3** — степень полного 2-D полинома (`--degree 3`).
- **10 коэф./ось** — число мономов `n_coef(3) = (3+1)(3+2)/2 = 10`; столько
  коэффициентов фитится **отдельно** по оси `θx` и по оси `θy`.
- **базис erfinv** — независимая переменная не сам `D`, а `u = erfinv(clip(D,±d_max))`.
  В этом базисе идеальная нож-модель почти линейна (`θ ≈ k·u`), поэтому полином
  ловит только остаточную нелинейность.
- **d_max=0.95** (`cfg.COMP_DMAX`) — двойная роль: (1) клип `D` перед `erfinv`,
  чтобы `erfinv(±1)=∞` не взрывался; (2) граница зоны валидности — точки с `|D|>0.95`
  в фит не берутся и моделью не предсказываются (без экстраполяции).
- **ADC_MAX=3500** (`cfg.ADC_MAX`) — на этом значении считается яркость
  `b=max(0,ADC_MAX−s)` и из неё `D`. Важно: модель обучается на той же `ADC_MAX`,
  что используется в рантайме `make_point`, иначе шкала `D` поедет.

```
точек: всего 83 → валидных 83 → в фите 83
```
Воронка отсева (`fit_from_points`): **всего** = все точки в файле; **валидных** —
прошедшие фильтр `P>0` **и** `|Dx|,|Dy|≤d_max` **и** `nz≥NZ_ANGLE_MIN` (≥3
засвеченных квадранта); **в фите** — оставшиеся после отбраковки выбросов `K·MAD`.
`83→83→83` значит: ни одна точка не отсеяна по насыщению/темноте и ни одна не
отбракована как выброс ввода углов. (Поэтому блоков `отфильтровано (...)` и
`выбросы K·MAD (...)` в выводе нет — они печатаются только при непустых списках.)

```
диапазон D: Dx∈[-0.521,+0.488]  Dy∈[-0.479,+0.492]  std(angle_y)=0.724
```
- **Dx∈[…], Dy∈[…]** — min/max разностного сигнала по **всем** точкам (`info["d_range"]`).
  Все значения внутри `±0.95` → насыщения нет, рабочая зона umfasst примерно
  половину полной шкалы `D`.
- **std(angle_y)=0.724** — СКО истинного угла `θy` по точкам, в **градусах**.
  Это индикатор 2-D покрытия: `0.724°` ≫ 0 значит, что стенд теперь качал и по `y`
  (раньше качал только по `x`, и тогда срабатывало предупреждение `std(angle_y)≈0`,
  делающее `y`- и перекрёстные члены недоопределёнными). Здесь предупреждения нет —
  данные двумерные, перекрёстные члены определимы.

## R.2. Таблица коэффициентов

```
коэффициенты (моном: coef_x  coef_y):
  1          -0.0173     -0.0112
  uy         +0.2661     +3.2502
  uy²        -0.0800     -0.3949
  uy³        -0.0137     -0.6594
  ux         +3.1789     -0.3772
  ux·uy      -0.0219     +0.1009
  ux·uy²     -0.1022     +0.8794
  ux²        +0.3584     +0.1856
  ux²·uy     +0.6262     +0.1091
  ux³        -0.2896     -0.4237
```
Каждая строка — один моном `ux^p · uy^q`; порядок задан `poly_terms(3)` и
**жёстко** совпадает с порядком в сохранённом `COMPENSATION.json`. Два столбца —
коэффициент для предсказания `θx` (`coef_x`) и для `θy` (`coef_y`). Предсказание:
`θx = Σ coef_x[k]·ux^p·uy^q`, аналогично `θy`. Как читать:

- **`ux` для θx = +3.179** и **`uy` для θy = +3.250** — доминирующие линейные
  члены, это «коэффициент усиления» нож-модели. Линеаризация ножа даёт наклон
  `w/(√2·FOC)·180/π = 4.06/(√2·50)·57.3 ≈ 3.29 °/ед.` — близко к фитнутым 3.18/3.25;
  расхождение и есть та систематика, которую добирают остальные члены.
- **Свободный член `1`** (−0.017 / −0.011°) — остаточное смещение нуля (механический
  перекос центра): почти ноль, центр выставлен хорошо.
- **Перекрёстные члены** (`ux` в θy = −0.377, `uy` в θx = +0.266, `ux·uy`, `ux·uy²`,
  `ux²·uy`) — связь осей (лёгкий поворот/перекос системы). Малы у θx (оси почти не
  повёрнуты), но у θy заметнее (`uy³=−0.659`, `ux·uy²=+0.879`) — по `y` реальный
  профиль сильнее отклоняется от идеального ножа.
- **Старшие члены `²`/`³`** — кривизна отклика, которую линейный нож не описывает.
  Замечание: на степени 3 это 10 коэф./ось при 83 точках (≈8 точек/коэф, выше
  минимума `COMP_MIN_POINTS_PER_COEF=3`), но кубические члены легко начинают
  подгонять шум — судить об их пользе нужно по LOO/hold-out (ниже), а не по их величине.

## R.3. Ошибки фита

```
обучающий RMSE: x=0.0254°  y=0.0352°
LOO RMSE (паспорт): x=0.0295°  y=0.0401°
```
- **обучающий RMSE** — СКО остатков на тех же точках, на которых фитили
  (`√mean(resid²)`). **Оптимистичная** оценка: модель «видела» эти точки, ошибка
  занижена.
- **LOO RMSE (паспорт)** — leave-one-out: для каждой точки модель как бы
  переобучается без неё и предсказывает её. Считается точно через диагональ
  hat-матрицы `r_i/(1−h_ii)` (без `N` перефитов). Это **честная** ошибка обобщения,
  её и записывают в паспорт модели (`model.loo_rmse_deg`). LOO (0.0295/0.0401) лишь
  чуть выше обучающей (0.0254/0.0352) — признак, что грубого переобучения на
  степени 3 нет.

## R.4. Блок «ПРОВЕРКА КОМПЕНСАЦИИ»

```
in-sample на 83 точках: RMSE x=0.0254° y=0.0352°  | max |ошибки| x=0.071° y=0.080°
```
`evaluate`: модель применяется ко всем валидным точкам (`|D|≤d_max & P>0`) и
сравнивается с истинными углами. **RMSE** совпадает с обучающим (все 83 точки и
были в фите). **max |ошибки|** — максимальная по модулю ошибка по оси: даже худшая
точка укладывается в ≈0.07–0.08°.

```
худшие точки (ключ: ошибка_x, ошибка_y):
  i73: +0.015°, +0.080°
  ...
```
Топ-5 точек, отсортированных по `max(|ошибка_x|, |ошибка_y|)`; ошибки **знаковые**
(`предсказание − истина`). Помогает увидеть, не выбивается ли конкретная точка
(кандидат на опечатку угла или плохую засветку).

```
hold-out 58/25 ×200: RMSE x=0.0314±0.0057°  y=0.0427±0.0065°
```
Out-of-sample проверка (`holdout_rmse`): 83 точки **200 раз** случайно бьются на
train/test в долях 70/30 → train=58, test=25. На каждом разбиении полином фитится
на train и проверяется на **невиданных** test. Печатается среднее ± СКО тестового
RMSE по 200 прогонам. Это самая реалистичная оценка точности на новых данных;
`±` — разброс из-за случайности разбиения. Значения (0.031/0.043) согласуются с
LOO — подтверждают отсутствие переобучения.

```
базовая нож-модель (w=4.06 мм, FOC=50): RMSE x=0.0860° y=0.0994°
выигрыш (нож/полином, hold-out): ×2.7 по x, ×2.3 по y
```
- **базовая нож-модель** — эталон «как было без компенсации»: чистая формула
  `θ = atan(w·erfinv(D)/(√2·FOC))` (`deflection_angles`), `w=4.06 мм` берётся из
  `CALIBRATE.json`, `FOC=50` из конфига. Считается на тех же валидных точках. Если
  калибровки нет — строка заменяется на «нет CALIBRATE.json — сравнение пропущено».
- **выигрыш** — отношение `RMSE_нож / RMSE_полином(hold-out)`:
  `0.0860/0.0314 ≈ 2.7`, `0.0994/0.0427 ≈ 2.3`. Полином снижает ошибку угла примерно
  в **2.7 раза по x** и **2.3 раза по y** относительно нескомпенсированной нож-модели.

## R.5. Короткий итог

Фит прошёл чисто: все 83 точки валидны, без выбросов и предупреждений, данные 2-D
(`std(angle_y)>0`), рабочая зона `|D|≲0.5` далеко от насыщения. Полином на степени 3
даёт честную (LOO/hold-out) ошибку ≈0.03°/0.04° против ≈0.086°/0.099° у нож-модели —
выигрыш ×2.7/×2.3. Доминируют линейные `ux`/`uy` (≈ нож-наклон), остальное добирает
систематику и связь осей. Полезность кубических членов стоит сверить с `--degree 2`
по **LOO**, а не по обучающему RMSE.
