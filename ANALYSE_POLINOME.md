# Анализ: способен ли полином определять угол при затемнении квадрантов

Вопрос: в датчике 4 квадранта засвечиваются лучом. Когда яркость есть на всех
четырёх — углы считаются в линейном режиме (это и делает компенсационный
полином). Способен ли полином определять угол отклонения, когда:

1. луч смещён в сторону и **один** квадрант затемнён (АЦП ≈ 3350);
2. затемнены **два** квадранта (засвет только двух)?

**Короткий ответ.** Один тёмный квадрант (~3350) — угол ещё физически
определим, но **не текущим полиномом** (он обучен только до ±2°) и с быстро
падающей точностью. Два тёмных квадранта — **нет**: как минимум одна ось
теряется принципиально (насыщение), и пайплайн уже помечает это состоянием
`lost`.

---

## 1. Почему наступает предел — насыщение

Весь метод — это **отношение** `D = (разность ярких)/(сумма)`, а связь с углом —
нож-модель `D = erf(√2·d/w)`; угол извлекается через `u = erfinv(D)`. Яркость
`b = max(0, ADC_MAX − raw)`, `ADC_MAX = 3500`:

- `raw = 3350 → b = 150` (≈4% от максимума);
- `raw ≥ 3500 → b = 0` (кламп — полная тьма).

Как только квадрант гаснет (`b → 0`), он выпадает и из числителя, и из
знаменателя — отношение `D` упирается в ±1 и **перестаёт зависеть от угла**:
луч можно двигать дальше, а `D` стоит. Информация об угле пропадает.

`erf` гасит чувствительность задолго до полного клампа. Усиление шума
`d(угол)/dD` (наклон полинома ≈ 3.2, по фактическим коэффициентам):

| \|D\| | угол  | шум D=0.01 → ошибка | режим                       |
|-------|-------|---------------------|-----------------------------|
| 0.30–0.60 | ≤1.2° | **0.03–0.04°**  | линейный, 4 квадранта       |
| 0.70  | ~2°   | 0.05°               | край обучения полинома      |
| 0.80  | ~2.5° | 0.06°               | один квадрант тускнеет      |
| 0.90  | ~3°   | **0.11°**           | один квадрант ≈ 3350        |
| 0.95  | —     | 0.19°               | граница `COMP_DMAX`         |
| 0.99  | —     | **0.78°**           | почти два тёмных            |

Шум одного измерения ~±0.03° (по разбросу нуля). У `|D| = 0.9` тот же шум АЦП
даёт уже ~0.11° — рост **экспоненциальный**.

---

## 2. Привязка «3350» к режимам (по реальным данным)

Из `DATA/MEASURE/MEASURE_v1.json` (там углы до 2.67°): самый тёмный квадрант
доходит до `raw` 3250–3350 только на углах **~2.1–2.67°**, где `|D| ≈ 0.68`.
Отсюда:

- тёмный квадрант **≈ 3350 ⇒ `|D| ≈ 0.7–0.9`, угол ≈ 2.5–3°+** — это **за
  пределом обучения** полинома (он снят до ±2°, `|D| ≲ 0.7`);
- `S_VAL_MIN = 3350` в `config.py` — это и есть **порог тьмы**: квадрант на 3350
  ровно на границе, где код перестаёт считать его «засвеченным»;
- `NZ_ANGLE_MIN = 3` — при <3 засвеченных квадрантах включается состояние
  `lost` (Задача №8): углы показываются как «—».

Эмпирическая связь «тёмный квадрант (raw) ↔ |D| ↔ угол», MEASURE_v1.json:

```
тёмный raw 2800-3000:  |D|≈0.40  угол≈1.2°
тёмный raw 3000-3150:  |D|≈0.53  угол≈1.6°
тёмный raw 3150-3250:  |D|≈0.61  угол≈1.8°
тёмный raw 3250-3350:  |D|≈0.68  угол≈2.1°  (до 2.67°)
```

---

## 3. Режим 1: один квадрант тёмный (~3350), три светятся

Это **диагональное** смещение — гаснет квадрант, противоположный направлению
луча. Физически **угол ещё определим**: 3 ярких + 1 тусклый (`b ≈ 150`, не ноль)
дают две независимые, ещё монотонные разности `Dx, Dy`. Но:

- **Текущим полиномом — нет.** `make_point` применяет полином только при
  `|D| ≤ COMP_DMAX = 0.95`, а обучен он лишь до `|D| ≈ 0.7`. На `|D| ~ 0.85` он
  **экстраполирует** → ошибка растёт быстро.
- Даже идеально обученный — точность падает в 3–5× (таблица §1).
- **Чтобы заработало:** доснять калибровку до ±3° (чтобы `|D| ~ 0.9` был
  *внутри* данных, а не экстраполяцией), поднять `COMP_DMAX`, возможно degree 4.
  Точность в этой зоне всё равно будет заметно хуже линейной.

---

## 4. Режим 2: два квадранта тёмные (светятся только два)

Принципиально хуже; зависит от геометрии:

- **Пара вдоль оси** (луч ушёл по +x → оба левых квадранта тёмные, оба правых
  яркие): ось `x` **насыщена** — `Dx → +1`, угол по x **невосстановим**
  (`erfinv(1) = ∞`). Перпендикулярная ось `y` ещё закодирована в двух ярких
  квадрантах (`Dy = (b4 − b3)/(b4 + b3)`) — её *в принципе* можно вытащить, но
  **не текущей 2D-моделью** (она обучена на 4-квадрантном режиме, `P` мал и
  шумен).
- **Пара по диагонали** — обе оси частично насыщены, ещё хуже.

В коде это уже отражено: `NZ_ANGLE_MIN = 3` → состояние `lost`, углы «—», точка
прижимается к краю дисплея по последнему направлению. То есть «2 квадранта»
система намеренно считает неизмеримой **по величине** (остаётся только
направление).

**Вывод по режиму 2:** полноценный 2D-угол полином дать не может. Максимум —
одна (перпендикулярная) ось спецмоделью на 2 квадранта; вдоль тёмной стороны
угол потерян физически.

---

## 5. Что с этим делать

1. **Расширить рабочий диапазон до 3-квадрантного режима:** калибровка до ±3°,
   `COMP_DMAX ↑` (~0.9), degree 4 — даст ещё ~0.5–1° диапазона ценой роста ошибки
   до ~0.1–0.2°. Дальше `|D| > 0.95` смысла нет (доминирует шум).
2. **Главный физический рычаг — размер пятна `w`.** `D = erf(√2·d/w)`: большее
   пятно насыщается *позже* (тот же `D` достигается при большем смещении) → все 4
   квадранта светятся в большем диапазоне углов, дольше держится линейный режим.
   Цена — меньше чувствительность у нуля. Если нужно мерить большие углы без
   потери квадрантов — расфокусировать / увеличить пятно.
3. **За пределом 4-квадрантного перекрытия** метод «разность/сумма» в принципе
   непригоден для 2D-угла. Варианты: другой обсёрвабл (абсолютные интенсивности
   квадрантов + геометрическая модель перекрытия пятна), либо принять, что
   2 квадранта = «только направление» (как сейчас в режиме `lost`).

---

## Сводка

| Режим | Квадрантов светится | Угол по физике | Текущий полином |
|-------|--------------------|----------------|-----------------|
| Линейный (`|D|≲0.7`, ≤±2°) | 4 | да, точно | **да, ~0.03–0.05°** |
| Один тёмный (~3350, ~3°) | 3 (+1 тусклый) | да, хуже | нет (экстраполяция); нужна докалибровка |
| Два тёмных | 2 | одна ось теряется | **нет** (состояние `lost`) |

Параметры из `config.py`: `ADC_MAX = 3500`, `S_VAL_MIN = 3350` (порог тьмы),
`COMP_DMAX = 0.95` (зона валидности полинома), `NZ_ANGLE_MIN = 3` (порог `lost`).
